# 算法岗刷题软件 - 全自动云端打包与 App 生成教程

本文档为您详细介绍**全自动云端打包方案**。您无需在本地电脑安装任何 Android Studio、Java JDK 或 Gradle（省去 10GB+ 繁杂的开发环境配置），全部由 **GitHub 云端服务器** 免费自动完成原生安卓 `.apk` 安装包的编译与发布！

---

## 目录
1. [方案核心优势](#一方案核心优势)
2. [三步全自动生成 Android APK 安装包](#二三步全自动生成-android-apk-安装包)
3. [如何在手机上安装与使用 APK](#三如何在手机上安装与使用-apk)
4. [方案对比：原生 APK 与 PWA 独立 App 该选哪种？](#四方案对比原生-apk-与-pwa-独立-app-该选哪种)
5. [后续更新题库：如何一键自动重新出包](#五后续更新题库如何一键自动重新出包)
6. [常见疑问解答](#六常见疑问解答)

---

## 一、方案核心优势

- 🚀 **零本地配置**：本地不需要下载 10GB+ 的 Android SDK，不占用电脑磁盘与内存。
- ☁️ **云端自动流水线**：基于 GitHub Actions，微软官方 Ubuntu 云端虚拟机全自动安装环境、编译 React 代码、转换 Capacitor 安卓项目并执行 Gradle 构建。
- 📦 **自动产出标准 APK**：构建完成后自动生成 `算法岗刷题-Android-Debug.apk`，手机或电脑直接点击即可下载安装。
- 🔄 **持续集成（CI/CD）**：每次你修改或补充题库，只要一键推送代码，云端就会全自动重新打出最新版的 APK。

---

## 二、三步全自动生成 Android APK 安装包

### 第一步：在 GitHub 上创建一个新仓库
1. 打开 [GitHub 官网](https://github.com/) 并登录您的账号。
2. 点击右上角的 **“+”** ➔ 选择 **“New repository”**（新建仓库）。
3. 填写仓库信息：
   - **Repository name**（仓库名称）：例如填写 `algo-quiz`。
   - **Visibility**（公开或私有）：勾选 **Public**（公开）或 **Private**（私有均可）。
   - ⚠️ **特别注意**：**不要勾选** “Add a README file” 或 “Add .gitignore”（保持为一个全新空白仓库）。
4. 点击绿色按钮 **“Create repository”**。
5. 复制页面上显示的 HTTPS 仓库地址（形如：`https://github.com/你的用户名/algo-quiz.git`）。

---

### 第二步：双击运行一键推送脚本
1. 在电脑的工作区目录 `算法岗笔试刷题` 下。
2. 找到并**双击运行**：
   👉 **`一键推送到GitHub云打包.bat`**
3. 按照黑窗口提示，粘贴你在第一步复制的 GitHub 仓库 HTTPS 地址，回车确认。
4. 脚本将全自动完成：
   - 初始化 Git 仓库与分支
   - 过滤大文件（自动排除本地生成的临时构建产物）
   - 提交全部 739 道题目与前端工程
   - 推送代码到 GitHub 云端

---

### 第三步：在 GitHub Actions 下载生成的 APK
1. 打开你的 GitHub 仓库网页。
2. 点击仓库顶部的 **【Actions】** 标签页。
3. 你会看到一个名为 **“全自动云端打包 (Android APK & Windows EXE)”** 的工作流正在自动运行（图标为黄色旋转圈）。
4. 等待 **2~3 分钟**（云端服务器正在拉取环境并构建）。
5. 构建完成后，图标会变成绿色勾勾（✅ **Success**）：
   - 点击该次运行记录进入详情页。
   - 滑动到页面最底部的 **【Artifacts】**（产物列表）。
   - 找到 **`算法岗刷题-Android-Debug.apk`**，点击即可直接下载！
   - （同时如果配置了 Release，可在仓库右侧 Releases 直接免解压下载 APK 文件）。

---

## 三、如何在手机上安装与使用 APK

1. **传输到手机**：
   - 可以通过微信/QQ 传输助手直接把下载好的 `app-debug.apk` 发送到手机上。
   - 也可以在手机自带浏览器中直接打开你的 GitHub 仓库页面下载该 APK。
2. **点击安装**：
   - 手机点击该 `.apk` 文件进行安装。
   - 如果系统提示“来自未知来源/未通过安全检查”，选择**“继续安装”**或**“允许来自此来源的应用”**（这是因为个人开发的测试包未购买几十万一年的商业数字签名证书，属于 Android 系统的常规安全提示，代码完全开源安全）。
3. **打开使用**：
   - 安装完成后，手机桌面上就会出现 **“算法岗刷题”** 的原生 App 图标！
   - 随时随地点击图标秒开刷题，**完全不需要开电脑、不需要连局域网**，739 道题目与公式离线运行！

---

## 四、方案对比：原生 APK 与 PWA 独立 App 该选哪种？

| 对比维度 | 📱 原生 Android APK 安装包 | 🌟 PWA 独立桌面 App (推荐) |
|---|---|---|
| **获取方式** | 下载几时兆的 `.apk` 文件手动安装 | 手机浏览器打开公网网址 ➔【添加到主屏幕】 |
| **设备支持** | 仅限 Android（苹果无法安装未签名的外部 APK） | **安卓、苹果 (iPhone/iPad) 通用支持** |
| **桌面图标** | 生成原生 App 图标 | 生成高清独立 App 图标 |
| **全屏无地址栏** | 默认全屏原生应用 | 默认全屏，无浏览器顶栏和底栏 |
| **离线可用** | 本地代码离线运行 | 内置 Service Worker，脱网离线运行 |
| **更新题库** | 需重新打包并下载覆盖安装 | **超轻量**：公网一更新，手机点开自动静默刷新题库 |

> **建议**：
> - 如果你是 **Android 手机** 且喜欢在系统设置里管理独立 APK，请使用本篇介绍的 **GitHub 云打包 APK**。
> - 如果你是 **苹果 iPhone / iPad** 或者不想每次手动安装 APK，强烈推荐直接使用 **PWA 方案**（只需把代码导入 Vercel 免费托管，在 Safari 点击“添加到主屏幕”即可秒变全屏独立 App）。

---

## 五、后续更新题库：如何一键自动重新出包

如果你未来在题库中新增了题目、修改了解析或优化了界面：
1. 本地修改完成后。
2. 再次双击根目录下的 **`一键推送到GitHub云打包.bat`**。
3. 脚本会自动将最新改动推送到 GitHub。
4. GitHub Actions 会再次全自动触发云端打包，几分钟后你就可以在 Actions 页面下载包含最新题库的全新 APK 安装包！

---

## 六、常见疑问解答

### Q1：GitHub Actions 云打包需要付费吗？
**完全免费！** GitHub 为每个个人账户每月提供 **2,000 分钟** 的免费 Actions 构建时长。编译一次仅消耗约 2 分钟，足够构建数百次，完全无需任何花费。

### Q2：推送代码时提示 Git 登录怎么办？
在黑窗口执行 `git push` 时，Windows 会自动弹出一个小窗口提示登录 GitHub：
- 推荐选择 **“Sign in with your browser”**（通过浏览器一键授权登录）。
- 浏览器授权通过后，黑窗口就会自动开始上传代码。

### Q3：苹果手机 (iPhone) 能安装这个 APK 吗？
不能。苹果 iOS 系统不允许安装外部 `.apk` 文件（那是安卓专用的），且苹果安装未上架 App 必须配置昂贵的苹果开发者证书。**苹果 iPhone 和 iPad 请使用 PWA 方案**：在 Safari 浏览器中点击【分享】➔【添加到主屏幕】，体验和原生 App 完全一样，同样全屏且支持离线！
