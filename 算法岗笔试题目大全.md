# 算法岗笔试题目大全（2025-2026）

> 涵盖：编程算法题、机器学习/深度学习理论题、大模型(LLM)专题、概率统计与数学推导、手撕代码

---

## 目录
- [一、编程算法题（LeetCode 高频）](#一编程算法题leetcode-高频)
- [二、机器学习理论题](#二机器学习理论题)
- [三、深度学习理论题](#三深度学习理论题)
- [四、大模型(LLM/NLP)专题](#四大模型llmnlp专题)
- [五、概率统计与数学推导题](#五概率统计与数学推导题)
- [六、手撕代码题](#六手撕代码题)
- [七、系统设计与场景题](#七系统设计与场景题)
- [八、备考资源汇总](#八备考资源汇总)

---

## 一、编程算法题（LeetCode 高频）

### 1.1 数组与双指针
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 1 | 两数之和 (Two Sum) | Easy | 哈希表 |
| 15 | 三数之和 (3Sum) | Medium | 排序+双指针 |
| 11 | 盛最多水的容器 | Medium | 双指针+贪心 |
| 42 | 接雨水 | Hard | 双指针/单调栈 |
| 53 | 最大子数组和 (Kadane算法) | Medium | DP/贪心 |
| 56 | 合并区间 | Medium | 排序+区间合并 |
| 31 | 下一个排列 | Medium | 双指针 |
| 48 | 旋转图像 | Medium | 矩阵操作 |
| 238 | 除自身以外数组的乘积 | Medium | 前缀积 |
| 41 | 缺失的第一个正数 | Hard | 原地哈希 |
| 287 | 寻找重复数 | Medium | 快慢指针 |
| 4 | 寻找两个正序数组的中位数 | Hard | 二分查找 |
| 128 | 最长连续序列 | Medium | 哈希集合 |
| 189 | 轮转数组 | Medium | 翻转数组 |

### 1.2 滑动窗口
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 3 | 无重复字符的最长子串 | Medium | 滑动窗口+哈希 |
| 76 | 最小覆盖子串 | Hard | 滑动窗口 |
| 239 | 滑动窗口最大值 | Hard | 单调队列 |
| 438 | 找到字符串中所有字母异位词 | Medium | 滑动窗口 |
| 567 | 字符串的排列 | Medium | 滑动窗口 |

### 1.3 链表
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 206 | 反转链表 | Easy | 指针操作 |
| 21 | 合并两个有序链表 | Easy | 归并 |
| 141 | 环形链表 | Easy | 快慢指针 |
| 142 | 环形链表 II | Medium | 快慢指针 |
| 25 | K 个一组翻转链表 | Hard | 链表翻转 |
| 19 | 删除链表的倒数第N个节点 | Medium | 快慢指针 |
| 23 | 合并K个升序链表 | Hard | 归并/堆 |
| 148 | 排序链表 | Medium | 归并排序 |
| 160 | 相交链表 | Easy | 双指针 |
| 234 | 回文链表 | Easy | 快慢指针+翻转 |
| 138 | 随机链表的复制 | Medium | 哈希/原地 |
| 146 | LRU 缓存 | Medium | 哈希+双向链表 |

### 1.4 二叉树
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 104 | 二叉树的最大深度 | Easy | DFS/BFS |
| 102 | 二叉树的层序遍历 | Medium | BFS |
| 236 | 二叉树的最近公共祖先 | Medium | DFS |
| 94 | 二叉树的中序遍历 | Easy | 递归/迭代 |
| 226 | 翻转二叉树 | Easy | DFS |
| 101 | 对称二叉树 | Easy | DFS |
| 543 | 二叉树的直径 | Easy | DFS |
| 98 | 验证二叉搜索树 | Medium | 中序遍历 |
| 114 | 二叉树展开为链表 | Medium | DFS+指针 |
| 105 | 从前序与中序遍历序列构造二叉树 | Medium | 递归 |
| 124 | 二叉树中的最大路径和 | Hard | DFS |
| 297 | 二叉树的序列化与反序列化 | Hard | BFS/DFS |

### 1.5 动态规划（重中之重）
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 70 | 爬楼梯 | Easy | 基础DP |
| 198 | 打家劫舍 | Medium | 线性DP |
| 300 | 最长递增子序列 (LIS) | Medium | DP/二分 |
| 1143 | 最长公共子序列 (LCS) | Medium | 二维DP |
| 5 | 最长回文子串 | Medium | DP/中心扩展 |
| 72 | 编辑距离 | Medium | 二维DP |
| 322 | 零钱兑换 | Medium | 完全背包 |
| 139 | 单词拆分 | Medium | DP+哈希 |
| 152 | 乘积最大子数组 | Medium | DP |
| 62 | 不同路径 | Medium | 路径DP |
| 64 | 最小路径和 | Medium | 路径DP |
| 121 | 买卖股票的最佳时机 | Easy | 贪心/DP |
| 122 | 买卖股票的最佳时机 II | Medium | 贪心/DP |
| 188 | 买卖股票的最佳时机 IV | Hard | 状态机DP |
| 10 | 正则表达式匹配 | Hard | DP |
| 32 | 最长有效括号 | Hard | 栈/DP |
| 416 | 分割等和子集 | Medium | 0/1背包 |
| 494 | 目标和 | Medium | 0/1背包 |
| 518 | 零钱兑换 II | Medium | 完全背包 |
| 312 | 戳气球 | Hard | 区间DP |
| 1049 | 最后一块石头的重量 II | Medium | 0/1背包 |

### 1.6 贪心算法
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 55 | 跳跃游戏 | Medium | 贪心 |
| 45 | 跳跃游戏 II | Medium | 贪心 |
| 435 | 无重叠区间 | Medium | 区间调度 |
| 452 | 用最少数量的箭引爆气球 | Medium | 区间贪心 |
| 763 | 划分字母区间 | Medium | 贪心 |
| 406 | 根据身高重建队列 | Medium | 贪心+排序 |
| 134 | 加油站 | Medium | 贪心 |

### 1.7 回溯法
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 46 | 全排列 | Medium | 回溯 |
| 78 | 子集 | Medium | 回溯 |
| 39 | 组合总和 | Medium | 回溯 |
| 22 | 括号生成 | Medium | 回溯 |
| 79 | 单词搜索 | Medium | DFS回溯 |
| 51 | N 皇后 | Hard | 回溯 |
| 17 | 电话号码的字母组合 | Medium | 回溯 |
| 131 | 分割回文串 | Medium | 回溯+DP |

### 1.8 二分查找
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 33 | 搜索旋转排序数组 | Medium | 二分 |
| 34 | 在排序数组中查找元素的第一个和最后一个位置 | Medium | 二分 |
| 35 | 搜索插入位置 | Easy | 二分 |
| 74 | 搜索二维矩阵 | Medium | 二分 |
| 153 | 寻找旋转排序数组中的最小值 | Medium | 二分 |
| 162 | 寻找峰值 | Medium | 二分 |

### 1.9 图论
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 200 | 岛屿数量 | Medium | DFS/BFS |
| 695 | 岛屿的最大面积 | Medium | DFS |
| 207 | 课程表 | Medium | 拓扑排序 |
| 210 | 课程表 II | Medium | 拓扑排序 |
| 994 | 腐烂的橘子 | Medium | 多源BFS |
| 547 | 省份数量 | Medium | 并查集/DFS |
| 399 | 除法求值 | Medium | 带权图 |

### 1.10 栈与队列
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 20 | 有效的括号 | Easy | 栈 |
| 155 | 最小栈 | Medium | 辅助栈 |
| 394 | 字符串解码 | Medium | 栈 |
| 84 | 柱状图中最大的矩形 | Hard | 单调栈 |
| 739 | 每日温度 | Medium | 单调栈 |
| 496 | 下一个更大元素 I | Easy | 单调栈 |

### 1.11 堆/优先队列
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 215 | 数组中的第K个最大元素 | Medium | 堆/快速选择 |
| 347 | 前 K 个高频元素 | Medium | 堆+哈希 |
| 295 | 数据流的中位数 | Hard | 对顶堆 |

### 1.12 字符串
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 28 | 找出字符串中第一个匹配项的下标 (KMP) | Easy | KMP |
| 49 | 字母异位词分组 | Medium | 哈希 |
| 647 | 回文子串 | Medium | 中心扩展/DP |
| 14 | 最长公共前缀 | Easy | 比较 |

---

## 二、机器学习理论题

### 2.1 基础概念
1. **什么是偏差-方差权衡(Bias-Variance Tradeoff)？如何解决过拟合和欠拟合？**
2. **生成模型与判别模型的区别？** 各举2-3个例子。
3. **L1正则化和L2正则化的区别？** 为什么L1能产生稀疏解？（从贝叶斯角度解释）
4. **交叉验证的原理？** K-fold 与 Leave-one-out 的区别和适用场景。
5. **解释"没有免费午餐"定理(No Free Lunch Theorem)。**
6. **什么是VC维(VC Dimension)？** 与模型复杂度有什么关系？
7. **解释最大似然估计(MLE)和最大后验估计(MAP)的区别。** MAP相当于加了什么正则？

### 2.2 经典算法
8. **逻辑回归(Logistic Regression)的损失函数推导。** 为什么用交叉熵不用MSE？
9. **SVM 的核函数原理是什么？** 什么是核技巧(Kernel Trick)？常用核函数有哪些？
10. **LR 和 SVM 的区别与联系？** 什么时候用LR，什么时候用SVM？
11. **决策树的剪枝策略有哪些？** 预剪枝和后剪枝的区别。
12. **GBDT 的原理？** 与 XGBoost 有什么区别？
13. **XGBoost 的泰勒展开是怎么做的？** 为什么用二阶泰勒展开？
14. **XGBoost vs LightGBM vs CatBoost 的区别？** 各自的优势和适用场景。
15. **随机森林(RF)和GBDT有什么区别？** Bagging和Boosting的核心区别。
16. **KNN的优缺点？** 如何选择K值？维度灾难如何影响KNN？
17. **K-Means 的原理和局限性？** 如何选择K值？与GMM的区别。
18. **PCA 的原理？** 特征值和特征向量的意义？PCA与SVD的关系。
19. **朴素贝叶斯分类器的"朴素"在哪？** 实际中这个假设合理吗？

### 2.3 模型评估
20. **Precision, Recall, F1-Score 的含义和计算。** 什么时候更关注Precision？什么时候更关注Recall？
21. **ROC曲线与AUC的含义。** AUC=0.5意味着什么？AUC如何计算？
22. **PR曲线和ROC曲线的区别？** 在类别不平衡时应该看哪个？
23. **混淆矩阵中各指标(TP/FP/TN/FN)如何理解？**
24. **如何处理类别不平衡问题？** 过采样(SMOTE)、欠采样、代价敏感学习等。

### 2.4 特征工程
25. **特征归一化(Normalization)和标准化(Standardization)的区别？** 什么时候需要？
26. **如何处理缺失值？** 不同方法的优缺点。
27. **如何进行特征选择？** 过滤法、包裹法、嵌入法。
28. **什么是特征交叉？** 在推荐系统中如何使用？
29. **高基数类别特征(High Cardinality)如何编码？** 目标编码(Target Encoding)的原理和风险。

---

## 三、深度学习理论题

### 3.1 基础网络
30. **常见激活函数(Sigmoid, ReLU, Tanh, GELU, SwiGLU)的特性比较。** 各自的梯度消失问题？
31. **为什么深度网络训练困难？** 梯度消失和梯度爆炸的原因和解决方法。
32. **BatchNorm 的原理和作用？** 训练和推理时的区别是什么？
33. **LayerNorm vs BatchNorm vs GroupNorm vs InstanceNorm 的区别？** 各适用于什么场景？
34. **RMSNorm 是什么？为什么大模型用 RMSNorm 替代 LayerNorm？**
35. **Dropout 防止过拟合的机制？** 训练和推理时如何处理？与贝叶斯的关系。
36. **常见优化器(SGD, Momentum, Adagrad, RMSProp, Adam, AdamW)的区别。** Adam为什么效果好？AdamW对比Adam改了什么？
37. **学习率调度(Learning Rate Schedule)策略有哪些？** Warmup的作用？Cosine Annealing的原理。
38. **权重初始化方法：Xavier vs Kaiming 初始化的区别和适用场景。**
39. **反向传播(Backpropagation)的完整推导。** 链式法则如何应用？
40. **残差连接(Residual Connection)为什么有效？** 从梯度流的角度解释。

### 3.2 CNN
41. **卷积层的参数量和计算量如何计算？**
42. **感受野(Receptive Field)的概念和计算方法。**
43. **1x1卷积的作用是什么？** 在Inception和ResNet中的用法。
44. **深度可分离卷积(Depthwise Separable Convolution)的原理。** 与标准卷积相比参数量减少多少？
45. **常见CNN架构演进：LeNet → AlexNet → VGG → GoogLeNet → ResNet → DenseNet → EfficientNet** 各自的创新点。
46. **转置卷积(Transposed Convolution)和上采样(Upsampling)的区别。**
47. **池化层(Pooling)的作用和类型。** 为什么现代网络减少了池化层？

### 3.3 RNN/Seq2Seq
48. **LSTM 的门控机制详解。** 遗忘门、输入门、输出门各自的作用。LSTM如何解决梯度消失？
49. **GRU 和 LSTM 的区别？** GRU为什么参数更少？
50. **Seq2Seq 模型的编码器-解码器结构。** Attention 机制如何改进Seq2Seq？
51. **为什么Transformer逐渐取代了RNN？** 并行化和长距离依赖的对比。

### 3.4 Transformer（核心重点）
52. **Self-Attention 的公式推导。** Q, K, V 的物理意义是什么？
53. **为什么 Attention 要除以 √d_k？** 从数值稳定性和梯度的角度解释。
54. **Multi-Head Attention 的作用？** 多头注意力比单头好在哪里？
55. **MHA vs MQA vs GQA 的区别。** GQA 在哪些模型中使用？
56. **位置编码的作用和类型。** 绝对位置编码 vs 相对位置编码。
57. **RoPE(旋转位置编码)的原理。** 数学推导？如何扩展上下文窗口(NTK-Aware Scaling)？
58. **ALiBi 的原理和优势。**
59. **Pre-Norm vs Post-Norm 的区别。** 为什么大模型倾向于用Pre-Norm？
60. **Flash Attention 的原理。** 如何减少IO操作？为什么能加速？
61. **KV Cache 的原理和作用。** 如何计算KV Cache的内存占用？
62. **Encoder-Only vs Decoder-Only vs Encoder-Decoder 架构的区别。** 分别适合什么任务？

---

## 四、大模型(LLM/NLP)专题

### 4.1 大模型训练
63. **预训练(Pre-training) → SFT(指令微调) → RLHF(人类反馈强化学习)的完整流程。**
64. **RLHF的原理？** Reward Model如何训练？PPO算法在其中的作用。
65. **DPO(Direct Preference Optimization)的原理？** 相比RLHF的优势。
66. **LoRA 的原理和实现。** 低秩分解怎么减少参数量？A、B矩阵的初始化方式。
67. **QLoRA 的改进点。** 4-bit量化 + LoRA 的组合。
68. **全参数微调 vs LoRA 微调 vs Prefix Tuning 的对比。**
69. **灾难性遗忘(Catastrophic Forgetting)的原因和解决方法。**
70. **分布式训练的常见策略：数据并行(DP)、模型并行(MP)、流水线并行(PP)、张量并行(TP)。** ZeRO优化分几个阶段？
71. **混合精度训练(FP16/BF16)的原理。** 为什么需要loss scaling？

### 4.2 大模型推理
72. **模型量化：PTQ vs QAT 的区别。** INT8/INT4量化的精度损失如何评估？
73. **推理加速技术总结：** KV Cache、Flash Attention、Paged Attention、投机采样(Speculative Decoding)。
74. **Paged Attention 的原理。** 如何解决KV Cache的内存碎片问题？
75. **如何计算大模型的显存需求？** 参数量 → FP16/INT8 → 推理/训练 所需显存。
76. **Token/s 和 Time-to-First-Token (TTFT) 的计算。**
77. **vLLM 的核心优化思路。**

### 4.3 大模型应用
78. **RAG(检索增强生成)的完整流程。** 向量检索 + 重排序 + 生成。
79. **RAG vs 微调：什么时候用RAG？什么时候微调更合适？**
80. **如何评估和缓解大模型的幻觉(Hallucination)？**
81. **Prompt Engineering 的常用技巧：** Few-shot、Chain-of-Thought(CoT)、Self-Consistency、Tree-of-Thought。
82. **Function Calling / Tool Use 的实现原理。**
83. **Agent(智能体)的架构设计：** ReAct模式、Multi-Agent协作。
84. **向量数据库选型(Milvus/Pinecone/FAISS)的对比。** 不同检索方式(余弦相似度/欧氏距离/内积)的区别。

### 4.4 经典NLP模型
85. **Word2Vec(CBOW vs Skip-gram)的原理。** 负采样(Negative Sampling)的作用。
86. **BERT的预训练任务(MLM + NSP)。** BERT是如何做双向编码的？
87. **GPT系列的演进：GPT-1 → GPT-2 → GPT-3 → GPT-4** 各自的关键改进。
88. **T5的"Text-to-Text"框架。**

---

## 五、概率统计与数学推导题

### 5.1 概率论
89. **经典问题：一枚硬币，已知正面概率为p，如何设计一个公平的随机事件？**
90. **蓄水池采样(Reservoir Sampling)：** 从数据流中等概率抽取K个样本的算法。
91. **绳子截断问题：** 一根绳子随机截成三段，能构成三角形的概率是多少？（1/4）
92. **生日悖论：** 23人中至少两人生日相同的概率？
93. **Coupon Collector问题：** 集齐N种优惠券的期望次数是多少？
94. **贝叶斯公式应用：** 医疗诊断问题——某疾病发病率1%，检测准确率99%，检测阳性后实际患病概率？
95. **条件概率陷阱：** 一个家庭有两个孩子，已知至少一个是男孩，两个都是男孩的概率？
96. **抽球问题：** 袋中有n个红球和m个白球，不放回抽k个球，恰好抽到i个红球的概率。
97. **几何概率：** 在一个正方形中随机取一个点，落在其内切圆中的概率。
98. **蒙提霍尔问题(三门问题)：** 换门和不换门的概率分析。

### 5.2 期望与方差
99. **均匀分布U[0,1]下，n个独立样本的最大值的期望是多少？** E[max] = n/(n+1)
100. **投掷骰子问题：** 投掷一个六面骰子，投出6的期望投掷次数？
101. **随机变量的协方差和相关系数的区别。**
102. **中心极限定理的直觉理解和应用。**
103. **大数定律和中心极限定理的区别。**

### 5.3 数学推导
104. **推导逻辑回归的梯度下降更新公式。**
105. **推导 Softmax 函数的梯度。**
106. **推导交叉熵损失函数的梯度。**
107. **推导 SVM 的对偶问题。**
108. **推导 BatchNorm 的反向传播公式。**
109. **推导 Self-Attention 的计算复杂度（为什么是O(n²d)）。**
110. **KL散度的定义和性质。** KL散度为什么不对称？与交叉熵的关系。
111. **信息增益和信息熵的关系。** 在决策树中的应用。
112. **EM算法的推导和直觉理解。** E步和M步各做了什么？

### 5.4 线性代数
113. **特征值分解和奇异值分解(SVD)的区别。**
114. **矩阵的秩、行列式、迹的意义。**
115. **正定矩阵的判定条件。** 在机器学习中的应用（如核函数）。
116. **矩阵求导的基本规则。** 向量对向量的求导。

---

## 六、手撕代码题

### 6.1 机器学习算子
117. **手写 Softmax（注意数值稳定性，减去最大值）**
```python
import numpy as np

def softmax(x):
    """带数值稳定性的Softmax"""
    x_max = np.max(x, axis=-1, keepdims=True)
    exp_x = np.exp(x - x_max)
    return exp_x / np.sum(exp_x, axis=-1, keepdims=True)
```

118. **手写交叉熵损失(Cross-Entropy Loss)**
```python
def cross_entropy_loss(y_pred, y_true):
    """二分类交叉熵"""
    epsilon = 1e-12
    y_pred = np.clip(y_pred, epsilon, 1 - epsilon)
    return -np.mean(y_true * np.log(y_pred) + (1 - y_true) * np.log(1 - y_pred))
```

119. **手写 MSE Loss**
```python
def mse_loss(y_pred, y_true):
    return np.mean((y_pred - y_true) ** 2)
```

120. **手写 Sigmoid 及其导数**
```python
def sigmoid(x):
    return 1 / (1 + np.exp(-x))

def sigmoid_derivative(x):
    s = sigmoid(x)
    return s * (1 - s)
```

121. **手写逻辑回归（训练+预测）**
```python
class LogisticRegression:
    def __init__(self, lr=0.01, epochs=1000):
        self.lr = lr
        self.epochs = epochs

    def fit(self, X, y):
        m, n = X.shape
        self.w = np.zeros(n)
        self.b = 0
        for _ in range(self.epochs):
            z = X @ self.w + self.b
            a = sigmoid(z)
            dw = (1/m) * X.T @ (a - y)
            db = (1/m) * np.sum(a - y)
            self.w -= self.lr * dw
            self.b -= self.lr * db

    def predict(self, X):
        return (sigmoid(X @ self.w + self.b) >= 0.5).astype(int)
```

122. **手写 K-Means 聚类**
```python
def kmeans(X, k, max_iters=100):
    # 随机初始化质心
    indices = np.random.choice(len(X), k, replace=False)
    centroids = X[indices]

    for _ in range(max_iters):
        # 分配簇
        distances = np.linalg.norm(X[:, None] - centroids[None, :], axis=2)
        labels = np.argmin(distances, axis=1)
        # 更新质心
        new_centroids = np.array([X[labels == i].mean(axis=0) for i in range(k)])
        if np.allclose(centroids, new_centroids):
            break
        centroids = new_centroids

    return labels, centroids
```

123. **手写 KNN 分类器**
```python
from collections import Counter

def knn_predict(X_train, y_train, x_query, k=5):
    distances = np.linalg.norm(X_train - x_query, axis=1)
    k_nearest = np.argsort(distances)[:k]
    k_labels = y_train[k_nearest]
    return Counter(k_labels).most_common(1)[0][0]
```

### 6.2 深度学习组件

124. **手写 LayerNorm**
```python
def layer_norm(x, gamma, beta, eps=1e-5):
    mean = np.mean(x, axis=-1, keepdims=True)
    var = np.var(x, axis=-1, keepdims=True)
    x_norm = (x - mean) / np.sqrt(var + eps)
    return gamma * x_norm + beta
```

125. **手写 RMSNorm**
```python
def rms_norm(x, gamma, eps=1e-6):
    rms = np.sqrt(np.mean(x ** 2, axis=-1, keepdims=True) + eps)
    return gamma * (x / rms)
```

126. **手写 Self-Attention (Scaled Dot-Product Attention)**
```python
def scaled_dot_product_attention(Q, K, V, mask=None):
    d_k = Q.shape[-1]
    scores = Q @ K.transpose(-2, -1) / np.sqrt(d_k)
    if mask is not None:
        scores = scores + mask * (-1e9)
    weights = softmax(scores)
    return weights @ V
```

127. **手写 Multi-Head Attention (PyTorch版)**
```python
import torch
import torch.nn as nn

class MultiHeadAttention(nn.Module):
    def __init__(self, d_model, num_heads):
        super().__init__()
        self.num_heads = num_heads
        self.d_k = d_model // num_heads
        self.W_q = nn.Linear(d_model, d_model)
        self.W_k = nn.Linear(d_model, d_model)
        self.W_v = nn.Linear(d_model, d_model)
        self.W_o = nn.Linear(d_model, d_model)

    def forward(self, Q, K, V, mask=None):
        B, L, _ = Q.size()
        Q = self.W_q(Q).view(B, L, self.num_heads, self.d_k).transpose(1, 2)
        K = self.W_k(K).view(B, -1, self.num_heads, self.d_k).transpose(1, 2)
        V = self.W_v(V).view(B, -1, self.num_heads, self.d_k).transpose(1, 2)

        scores = Q @ K.transpose(-2, -1) / (self.d_k ** 0.5)
        if mask is not None:
            scores = scores.masked_fill(mask == 0, float('-inf'))
        attn = torch.softmax(scores, dim=-1)
        out = (attn @ V).transpose(1, 2).contiguous().view(B, L, -1)
        return self.W_o(out)
```

128. **手写 Top-K 采样 和 Top-P (Nucleus) 采样**
```python
def top_k_sampling(logits, k, temperature=1.0):
    logits = logits / temperature
    top_k_vals, top_k_idx = torch.topk(logits, k)
    probs = torch.softmax(top_k_vals, dim=-1)
    idx = torch.multinomial(probs, 1)
    return top_k_idx[idx]

def top_p_sampling(logits, p, temperature=1.0):
    logits = logits / temperature
    sorted_logits, sorted_idx = torch.sort(logits, descending=True)
    cum_probs = torch.cumsum(torch.softmax(sorted_logits, dim=-1), dim=-1)
    mask = cum_probs - torch.softmax(sorted_logits, dim=-1) >= p
    sorted_logits[mask] = float('-inf')
    probs = torch.softmax(sorted_logits, dim=-1)
    idx = torch.multinomial(probs, 1)
    return sorted_idx[idx]
```

### 6.3 经典数据结构实现

129. **手写快速排序（Quick Sort）**
```python
def quicksort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    return quicksort(left) + middle + quicksort(right)
```

130. **手写归并排序（Merge Sort）**
```python
def merge_sort(arr):
    if len(arr) <= 1:
        return arr
    mid = len(arr) // 2
    left = merge_sort(arr[:mid])
    right = merge_sort(arr[mid:])
    return merge(left, right)

def merge(left, right):
    result = []
    i = j = 0
    while i < len(left) and j < len(right):
        if left[i] <= right[j]:
            result.append(left[i]); i += 1
        else:
            result.append(right[j]); j += 1
    result.extend(left[i:])
    result.extend(right[j:])
    return result
```

131. **手写堆排序（Heap Sort）**
```python
def heapify(arr, n, i):
    largest = i
    l, r = 2*i + 1, 2*i + 2
    if l < n and arr[l] > arr[largest]: largest = l
    if r < n and arr[r] > arr[largest]: largest = r
    if largest != i:
        arr[i], arr[largest] = arr[largest], arr[i]
        heapify(arr, n, largest)

def heap_sort(arr):
    n = len(arr)
    for i in range(n//2 - 1, -1, -1):
        heapify(arr, n, i)
    for i in range(n-1, 0, -1):
        arr[0], arr[i] = arr[i], arr[0]
        heapify(arr, i, 0)
    return arr
```

132. **手写二分查找**
```python
def binary_search(arr, target):
    left, right = 0, len(arr) - 1
    while left <= right:
        mid = (left + right) // 2
        if arr[mid] == target: return mid
        elif arr[mid] < target: left = mid + 1
        else: right = mid - 1
    return -1
```

133. **手写 LRU Cache**
```python
from collections import OrderedDict

class LRUCache:
    def __init__(self, capacity):
        self.cache = OrderedDict()
        self.capacity = capacity

    def get(self, key):
        if key not in self.cache: return -1
        self.cache.move_to_end(key)
        return self.cache[key]

    def put(self, key, value):
        if key in self.cache:
            self.cache.move_to_end(key)
        self.cache[key] = value
        if len(self.cache) > self.capacity:
            self.cache.popitem(last=False)
```

134. **手写并查集(Union-Find)**
```python
class UnionFind:
    def __init__(self, n):
        self.parent = list(range(n))
        self.rank = [0] * n

    def find(self, x):
        if self.parent[x] != x:
            self.parent[x] = self.find(self.parent[x])  # 路径压缩
        return self.parent[x]

    def union(self, x, y):
        px, py = self.find(x), self.find(y)
        if px == py: return
        if self.rank[px] < self.rank[py]: px, py = py, px
        self.parent[py] = px
        if self.rank[px] == self.rank[py]: self.rank[px] += 1
```

---

## 七、系统设计与场景题

### 7.1 推荐系统
135. **设计一个新闻推荐系统的技术架构。** 召回→粗排→精排→重排 的流程。
136. **推荐系统中如何解决冷启动问题？**
137. **如何评估推荐系统的效果？** 在线指标(CTR/CVR) vs 离线指标(AUC/NDCG)。

### 7.2 搜索与广告
138. **搜索引擎的排序模型如何设计？** 从BM25到Learning-to-Rank到BERT重排。
139. **广告CTR预估模型的演进。** LR → Wide&Deep → DeepFM → DIN。
140. **如何设计一个实时竞价(RTB)系统？**

### 7.3 NLP应用
141. **如何设计一个客服对话系统(Chatbot)？** 意图识别 → 槽位填充 → 对话管理 → 回复生成。
142. **如何设计一个文本分类系统？** 从特征工程到深度学习方案。
143. **如何做大规模文本去重？** SimHash、MinHash 的原理。

### 7.4 CV应用
144. **目标检测的经典方法：RCNN → Fast RCNN → Faster RCNN → YOLO → SSD 的演进。**
145. **语义分割 vs 实例分割 vs 全景分割的区别。**
146. **如何设计一个图像检索系统？**

### 7.5 模型部署
147. **如何将一个训练好的模型部署到生产环境？** 模型导出 → 服务化(TFServing/TorchServe/Triton) → 监控。
148. **A/B 测试的原理和常见坑。** 如何判断实验结果是否显著？
149. **模型的版本管理和持续迭代。**

---

## 八、备考资源汇总

### 8.1 刷题平台
| 平台 | 特点 | 链接 |
|------|------|------|
| LeetCode | 基础算法，Hot 100 必刷 | leetcode.cn |
| 牛客网 | 大厂真题，ACM模式 | nowcoder.com |
| CodeTop | 按公司筛选高频面试题 | codetop.cc |
| 卡码网 | 大厂笔试真题，30+公司 | kamacoder.com |
| Deep-ML | ML/DL实战编码练习 | deep-ml.com |

### 8.2 必读书籍/资料
| 资料 | 内容 |
|------|------|
| 《统计学习方法》李航 | 机器学习经典教材 |
| 《动手学深度学习》李沐 | 深度学习入门首选 |
| 《百面机器学习》| ML面试题系统梳理 |
| 《深度学习500问》| DL知识体系 |
| 《代码随想录》| 算法刷题指南（有视频讲解）|
| 《剑指Offer》| 经典编程题集 |

### 8.3 GitHub 开源项目
| 项目 | 内容 |
|------|------|
| `imhuay/Algorithm_Interview_Notes-Chinese` | 万星面试笔记 |
| `NLP-LOVE/ML-NLP` | ML/NLP面试宝典 |
| `wdndev/llm_interview_note` | LLM面试笔记 |
| `DolbyUUU/Awesome-LLM-Interview-Questions` | 大模型面试题合集 |
| `Campus_recruitment_interview_questions` | 校招真题汇总 |

### 8.4 各大厂笔试特点
| 公司 | 笔试特点 |
|------|----------|
| 字节跳动 | 4道编程题，难度中等偏上，偏爱DP和模拟 |
| 阿里巴巴 | 偏爱DP和复杂逻辑，空间限制严格 |
| 腾讯 | 5道编程题，题量大，需要速度 |
| 华为 | 3道编程题，注重ACM输入输出，场景包装强 |
| 美团 | 5道编程题，偏重贪心和模拟 |
| 百度 | 选择题+编程题，ML基础选择题占比大 |
| 京东 | 选择题+编程题，数据结构基础考察多 |
| 快手 | 3-4道编程题，字符串和DP高频 |

---

> **刷题策略总结**：
> 1. 按专题分类刷题（数组→链表→树→DP→贪心→图）
> 2. 先刷 LeetCode Hot 100 + 剑指 Offer
> 3. 用 CodeTop 针对目标公司刷高频题
> 4. 练习 ACM 模式输入输出（牛客/卡码网）
> 5. ML/DL 理论按模块整理，重在理解"为什么"
> 6. 手撕代码每天练1-2题，重点练 Attention、Softmax、LR、排序
