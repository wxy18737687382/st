# 算法岗笔试题目大全（补充篇）

> 在原有 149+ 题基础上新增 **250+ 题**，涵盖：进阶编程题、CV/推荐系统/多模态专题、强化学习与生成模型、计算机基础、智力题、SQL题

---

## 目录
- [一、进阶编程算法题](#一进阶编程算法题)
- [二、计算机视觉(CV)专题](#二计算机视觉cv专题)
- [三、推荐系统/搜索/广告专题](#三推荐系统搜索广告专题)
- [四、多模态模型专题](#四多模态模型专题)
- [五、强化学习与生成模型专题](#五强化学习与生成模型专题)
- [六、计算机基础选择题](#六计算机基础选择题)
- [七、SQL高频题目](#七sql高频题目)
- [八、经典智力题与逻辑推理题](#八经典智力题与逻辑推理题)
- [九、更多手撕代码题](#九更多手撕代码题)
- [十、更多概率统计与数学题](#十更多概率统计与数学题)

---

## 一、进阶编程算法题

### 1.1 位运算
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 136 | 只出现一次的数字 | Easy | 异或 |
| 137 | 只出现一次的数字 II | Medium | 位运算/状态机 |
| 260 | 只出现一次的数字 III | Medium | 异或+分组 |
| 191 | 位1的个数 | Easy | n&(n-1) |
| 461 | 汉明距离 | Easy | 异或+计数 |
| 338 | 比特位计数 | Easy | DP+位运算 |
| 371 | 两整数之和 | Medium | 位运算模拟加法 |
| 190 | 颠倒二进制位 | Easy | 位操作 |
| 268 | 丢失的数字 | Easy | 异或/数学 |
| 231 | 2的幂 | Easy | n&(n-1)==0 |

### 1.2 前缀和与差分数组
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 303 | 区域和检索 - 数组不可变 | Easy | 一维前缀和 |
| 304 | 二维区域和检索 - 矩阵不可变 | Medium | 二维前缀和 |
| 560 | 和为K的子数组 | Medium | 前缀和+哈希 |
| 974 | 和可被K整除的子数组 | Medium | 前缀和+取模 |
| 523 | 连续的子数组和 | Medium | 前缀和+哈希 |
| 1109 | 航班预订统计 | Medium | 差分数组 |
| 1094 | 拼车 | Medium | 差分数组 |
| 370 | 区间加法 | Medium | 差分数组 |
| 1480 | 一维数组的动态和 | Easy | 前缀和基础 |

### 1.3 更多单调栈/单调队列
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 503 | 下一个更大元素 II | Medium | 单调栈+循环 |
| 907 | 子数组的最小值之和 | Medium | 单调栈 |
| 316 | 去除重复字母 | Medium | 单调栈+贪心 |
| 402 | 移掉K位数字 | Medium | 单调栈 |
| 862 | 和至少为K的最短子数组 | Hard | 单调队列 |

### 1.4 图论进阶
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 743 | 网络延迟时间 | Medium | Dijkstra |
| 787 | K站中转内最便宜的航班 | Medium | Bellman-Ford/DP |
| 1584 | 连接所有点的最小费用 | Medium | Prim/Kruskal(MST) |
| 684 | 冗余连接 | Medium | 并查集 |
| 685 | 冗余连接 II | Hard | 并查集+有向图 |
| 1168 | 水资源分配优化 | Hard | MST |
| 332 | 重新安排行程 | Hard | 欧拉路径 |
| 785 | 判断二分图 | Medium | BFS/DFS染色 |
| 886 | 可能的二分法 | Medium | 二分图判定 |
| 1135 | 最低成本联通所有城市 | Medium | MST |
| 133 | 克隆图 | Medium | BFS/DFS |
| 310 | 最小高度树 | Medium | 拓扑排序 |
| 329 | 矩阵中的最长递增路径 | Hard | DFS+记忆化 |

### 1.5 字典树(Trie)
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 208 | 实现 Trie (前缀树) | Medium | Trie 实现 |
| 211 | 添加与搜索单词 | Medium | Trie + DFS |
| 212 | 单词搜索 II | Hard | Trie + 回溯 |
| 421 | 数组中两个数的最大异或值 | Medium | Trie(二进制) |
| 648 | 单词替换 | Medium | Trie 前缀匹配 |
| 720 | 词典中最长的单词 | Medium | Trie + BFS |

### 1.6 线段树/树状数组
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 307 | 区域和检索 - 数组可修改 | Medium | 树状数组/线段树 |
| 315 | 计算右侧小于当前元素的个数 | Hard | 树状数组/归并 |
| 493 | 翻转对 | Hard | 归并/树状数组 |
| 327 | 区间和的个数 | Hard | 归并/线段树 |
| 218 | 天际线问题 | Hard | 线段树/堆 |

### 1.7 更多动态规划
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 97 | 交错字符串 | Medium | 二维DP |
| 115 | 不同的子序列 | Hard | 二维DP |
| 221 | 最大正方形 | Medium | 矩阵DP |
| 85 | 最大矩形 | Hard | 单调栈+DP |
| 354 | 俄罗斯套娃信封问题 | Hard | LIS(二分优化) |
| 368 | 最大整除子集 | Medium | DP+排序 |
| 91 | 解码方法 | Medium | 线性DP |
| 343 | 整数拆分 | Medium | 数学/DP |
| 279 | 完全平方数 | Medium | 完全背包/BFS |
| 377 | 组合总和 IV | Medium | DP |
| 474 | 一和零 | Medium | 二维背包 |
| 583 | 两个字符串的删除操作 | Medium | LCS变体 |
| 714 | 买卖股票的最佳时机含手续费 | Medium | 状态机DP |
| 309 | 买卖股票的最佳时机含冷冻期 | Medium | 状态机DP |
| 1547 | 切棍子的最小成本 | Hard | 区间DP |
| 87 | 扰乱字符串 | Hard | 记忆化搜索 |
| 174 | 地下城游戏 | Hard | 逆向DP |
| 514 | 自由之路 | Hard | 状态DP |
| 123 | 买卖股票最佳时机III | Hard | 状态机DP |

### 1.8 更多回溯与DFS
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 40 | 组合总和 II | Medium | 去重回溯 |
| 47 | 全排列 II | Medium | 去重回溯 |
| 90 | 子集 II | Medium | 去重回溯 |
| 93 | 复原 IP 地址 | Medium | 回溯+剪枝 |
| 37 | 解数独 | Hard | 回溯 |
| 52 | N 皇后 II | Hard | 回溯+计数 |
| 491 | 非递减子序列 | Medium | 回溯 |
| 332 | 重新安排行程 | Hard | 回溯+贪心 |

### 1.9 更多二分查找
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 875 | 爱吃香蕉的珂珂 | Medium | 二分答案 |
| 1011 | 在D天内送达包裹的能力 | Medium | 二分答案 |
| 410 | 分割数组的最大值 | Hard | 二分答案 |
| 378 | 有序矩阵中第K小的元素 | Medium | 二分+计数 |
| 240 | 搜索二维矩阵 II | Medium | Z字形查找 |
| 69 | x 的平方根 | Easy | 二分 |
| 367 | 有效的完全平方数 | Easy | 二分 |
| 658 | 找到 K 个最接近的元素 | Medium | 二分 |

### 1.10 更多哈希与设计
| # | 题目 | 难度 | 考察点 |
|---|------|------|--------|
| 380 | O(1)时间插入、删除和获取随机元素 | Medium | 哈希+数组 |
| 460 | LFU 缓存 | Hard | 哈希+双链表 |
| 895 | 最大频率栈 | Hard | 哈希+栈 |
| 706 | 设计哈希映射 | Easy | 哈希实现 |
| 705 | 设计哈希集合 | Easy | 哈希实现 |
| 341 | 扁平化嵌套列表迭代器 | Medium | 栈/递归 |

---

## 二、计算机视觉(CV)专题

### 2.1 基础理论
1. **卷积操作的输出尺寸计算公式？** $O = \lfloor\frac{I - K + 2P}{S}\rfloor + 1$，各参数含义。
2. **空洞卷积(Dilated/Atrous Convolution)的原理和作用？** 如何在不增加参数量的情况下扩大感受野？
3. **可变形卷积(Deformable Convolution)的原理？** 与标准卷积的区别。
4. **深度可分离卷积的参数量对比标准卷积减少了多少？** 具体计算。
5. **Group Convolution 的原理和动机？** AlexNet中的使用，与深度可分离卷积的关系。
6. **Squeeze-and-Excitation (SE) 模块的原理？** 通道注意力机制。
7. **CBAM (Convolutional Block Attention Module) 的结构？** 通道注意力+空间注意力。
8. **Feature Pyramid Network (FPN) 的设计思路？** 多尺度特征融合如何实现？

### 2.2 目标检测
9. **Anchor-based 和 Anchor-free 检测器的区别？** 各自的优缺点。
10. **YOLO 系列的演进 (v1→v11)：** 每个版本的核心改进是什么？
11. **Faster R-CNN 的架构：** RPN 的工作原理？如何生成候选区域？
12. **ROI Pooling 和 ROI Align 的区别？** 为什么 ROI Align 更精确？
13. **NMS (非极大值抑制) 的算法流程？手写实现。** Soft-NMS 改进了什么？
14. **Focal Loss 的推导和作用？** α和γ参数的意义？
15. **DETR 的核心思想？** 如何用 Transformer 做端到端目标检测？二部匹配(Hungarian Matching)。
16. **IoU / GIoU / DIoU / CIoU 的定义和区别？**
17. **小目标检测的难点和常见解决方案？** 多尺度训练、特征金字塔、高分辨率输入。

### 2.3 图像分割
18. **FCN (Fully Convolutional Network) 的核心思想？** 为什么将全连接层替换为卷积层？
19. **U-Net 的编码器-解码器结构和 skip connection 的作用？**
20. **DeepLab 系列中空洞空间金字塔池化(ASPP)的作用？**
21. **Mask R-CNN 的结构？** 如何在 Faster R-CNN 基础上增加分割分支？
22. **SAM (Segment Anything Model) 的架构和 Prompt 机制？** 点击、框、文字三种提示方式。
23. **语义分割的常用损失函数？** Cross Entropy、Dice Loss、Lovász Loss。
24. **语义分割 vs 实例分割 vs 全景分割的定义和区别？**

### 2.4 图像生成与其他
25. **数据增强的常见方法？** 翻转、旋转、裁剪、Mixup、CutMix、Mosaic。
26. **迁移学习的策略？** 冻结骨干网络 vs 微调全部参数。什么时候应该怎么做？
27. **模型蒸馏(Knowledge Distillation)的原理？** 软标签(Soft Labels)的作用。
28. **模型剪枝(Pruning)的类型？** 结构化剪枝 vs 非结构化剪枝。
29. **TensorRT 加速推理的原理？** 算子融合、精度量化、动态批处理。

### 2.5 CV手撕代码
30. **手写 IoU 计算**
```python
def compute_iou(box1, box2):
    """box format: [x1, y1, x2, y2]"""
    x1 = max(box1[0], box2[0])
    y1 = max(box1[1], box2[1])
    x2 = min(box1[2], box2[2])
    y2 = min(box1[3], box2[3])
    
    inter = max(0, x2 - x1) * max(0, y2 - y1)
    area1 = (box1[2] - box1[0]) * (box1[3] - box1[1])
    area2 = (box2[2] - box2[0]) * (box2[3] - box2[1])
    union = area1 + area2 - inter
    
    return inter / union if union > 0 else 0
```

31. **手写 NMS (非极大值抑制)**
```python
def nms(boxes, scores, iou_threshold=0.5):
    """boxes: [[x1,y1,x2,y2], ...], scores: [s1,s2,...]"""
    order = sorted(range(len(scores)), key=lambda i: scores[i], reverse=True)
    keep = []
    while order:
        i = order.pop(0)
        keep.append(i)
        order = [j for j in order if compute_iou(boxes[i], boxes[j]) < iou_threshold]
    return keep
```

32. **手写双线性插值(Bilinear Interpolation)**
```python
def bilinear_interpolate(img, x, y):
    x0, y0 = int(x), int(y)
    x1, y1 = x0 + 1, y0 + 1
    dx, dy = x - x0, y - y0
    
    val = (img[y0, x0] * (1-dx) * (1-dy) +
           img[y0, x1] * dx * (1-dy) +
           img[y1, x0] * (1-dx) * dy +
           img[y1, x1] * dx * dy)
    return val
```

33. **手写 2D 卷积操作**
```python
def conv2d(image, kernel, stride=1, padding=0):
    H, W = image.shape
    kH, kW = kernel.shape
    image = np.pad(image, padding)
    oH = (H + 2*padding - kH) // stride + 1
    oW = (W + 2*padding - kW) // stride + 1
    output = np.zeros((oH, oW))
    for i in range(oH):
        for j in range(oW):
            region = image[i*stride:i*stride+kH, j*stride:j*stride+kW]
            output[i, j] = np.sum(region * kernel)
    return output
```

---

## 三、推荐系统/搜索/广告专题

### 3.1 召回阶段
34. **双塔模型(DSSM/YouTube DNN)的架构和训练方式？** 负采样策略有哪些？
35. **向量检索(ANN)的常见方法？** HNSW、IVF、PQ 的原理和对比。
36. **Item2Vec 和 Node2Vec 的原理？** 如何将序列行为转化为 Embedding？
37. **多路召回和单路召回的优缺点？** 如何融合多路召回的结果？
38. **召回阶段如何解决冷启动问题？** 新用户和新物品分别如何处理？
39. **MIND(Multi-Interest Network with Dynamic routing)的核心思想？** 多兴趣召回。
40. **GraphSAGE 在召回中的应用？** 如何利用图结构信息？

### 3.2 排序阶段
41. **Wide & Deep 模型的设计动机？** Wide 部分和 Deep 部分分别学什么？
42. **DeepFM 的架构？** FM 组件如何与 DNN 组件结合？
43. **DCN/DCNv2 的显式特征交叉层如何工作？** Cross Network 的数学表达。
44. **DIN (Deep Interest Network) 的核心创新？** 注意力机制如何建模用户兴趣？
45. **DIEN (Deep Interest Evolution Network) 的兴趣演化模块？** GRU + AUGRU。
46. **多任务学习模型(MTL)：** Shared-Bottom vs MMoE vs PLE 的区别。
47. **如何解决多目标学习中的跷跷板(Seesaw)现象？**
48. **ESMM (Entire Space Multi-Task Model) 如何解决 CVR 的样本选择偏差？**
49. **特征离散化(Bucketization)的作用和方法？** 等宽、等频、自适应。
50. **Embedding 层的理解？** 为什么推荐系统中 Embedding 如此重要？

### 3.3 重排与策略
51. **重排阶段的目标是什么？** 与精排的区别。
52. **多样性(Diversity)和相关性(Relevance)的权衡？** MMR算法。
53. **EE(Explore-Exploit)问题在推荐中如何解决？** ε-greedy、UCB、Thompson Sampling。
54. **点击率(CTR)校准的必要性？** Platt Scaling、Isotonic Regression。
55. **推荐系统中常见的 Bias 有哪些？** 位置偏差、流行度偏差、选择偏差。如何去偏？

### 3.4 评估与工程
56. **AUC 和 GAUC 的区别？** 为什么推荐系统要用 GAUC？
57. **NDCG 的计算方法和适用场景？**
58. **如何保证离线训练和在线推理的特征一致性？** Feature Store 的设计。
59. **A/B 测试中如何判断结果的统计显著性？** p-value、置信区间。
60. **如何设计一个完整的推荐系统架构？** 数据流 → 特征 → 召回 → 排序 → 重排 → 展示。

### 3.5 广告系统
61. **eCPM(有效千次展示收入)的计算？** eCPM = CTR × bid × 1000。
62. **广告竞价机制：GSP vs VCG？** 各自的特性。
63. **Lookalike(相似人群扩展)的原理？**
64. **DPA(动态商品广告)系统的设计？**

---

## 四、多模态模型专题

### 4.1 ViT系列
65. **ViT 的 Patch Embedding 具体如何实现？** 计算过程和参数量。
66. **ViT 为什么需要 CLS Token？** CLS Token 的作用。
67. **ViT 缺少归纳偏置(Inductive Bias)意味着什么？** 与 CNN 的对比。
68. **DeiT (Data-efficient Image Transformers) 的改进？** 蒸馏 Token 的作用。
69. **Swin Transformer 的窗口注意力(Window Attention)和移位窗口(Shifted Window)？**

### 4.2 CLIP与对比学习
70. **CLIP 的 InfoNCE Loss 详细推导？** 温度系数 τ 的作用和最优值。
71. **CLIP 如何实现 Zero-Shot 分类？** 具体流程。
72. **CLIP 的局限性？** 对组合推理、计数等的弱项。
73. **SigLIP 对比 CLIP 改了什么？** Sigmoid Loss vs Softmax Loss。
74. **OpenCLIP 与原始 CLIP 的区别？** 开源复现的挑战。

### 4.3 VLM(视觉语言模型)
75. **LLaVA 的架构和训练流程？** 两阶段训练策略。
76. **BLIP-2 的 Q-Former 结构详解？** 如何从视觉特征中提取信息？
77. **InternVL / Qwen-VL 等国产 VLM 的架构特点？**
78. **多模态模型中的 Cross-Attention 和 Self-Attention 在融合中的应用？**
79. **视觉 Token 压缩技术？** Token Merging、动态分辨率。
80. **VLM 的幻觉(Hallucination)问题和缓解方法？** RLHF-V、直接偏好优化。
81. **多模态 Agent 的架构设计？** 视觉理解 + 工具调用 + 规划。
82. **OCR 和文档理解在 VLM 中的挑战？** 表格、公式、长文档。

---

## 五、强化学习与生成模型专题

### 5.1 强化学习基础
83. **马尔可夫决策过程(MDP)的五元组定义？** (S, A, P, R, γ)
84. **值函数 V(s) 和动作值函数 Q(s,a) 的区别？** Bellman 方程推导。
85. **Model-based vs Model-free 方法的区别？**
86. **On-policy vs Off-policy 的区别？** SARSA vs Q-Learning。
87. **Policy Gradient 定理的推导？** REINFORCE 算法。
88. **Actor-Critic 方法的核心思想？** Advantage Function 的作用。
89. **PPO(Proximal Policy Optimization)的 Clip 机制详解。** 为什么不用 TRPO？
90. **探索与利用(Exploration vs Exploitation)的策略？** ε-greedy, UCB, Thompson Sampling。
91. **Reward Shaping 的挑战？** 稀疏奖励问题如何解决？

### 5.2 RLHF与LLM对齐
92. **RLHF 的完整流程详解：** 1)SFT → 2)Reward Model训练 → 3)PPO微调。
93. **Reward Model 的训练数据如何构造？** 人类偏好对比数据(chosen vs rejected)。
94. **DPO vs PPO 的数学原理对比。** DPO 如何避免训练 Reward Model？
95. **GRPO(Group Relative Policy Optimization)的原理？** DeepSeek 的方法。
96. **KL 散度在 RLHF 中的作用？** 为什么要对参考模型做 KL 约束？
97. **Reward Hacking 现象及其缓解方法？**
98. **Constitutional AI (CAI) 的思想？** AI 自我对齐。

### 5.3 GAN
99. **GAN 的原始损失函数推导？** Min-Max 博弈的数学表达。
100. **GAN 训练不稳定的原因？** 模式坍塌(Mode Collapse)和梯度消失。
101. **WGAN 的改进原理？** Wasserstein距离 vs JS散度。WGAN-GP 中梯度惩罚的作用。
102. **Conditional GAN (cGAN) 的原理？** 如何控制生成内容？
103. **CycleGAN 的循环一致性损失？** 无配对图像翻译。
104. **StyleGAN 的风格混合(Style Mixing)机制？**
105. **GAN 的评估指标？** FID(Fréchet Inception Distance)、IS(Inception Score) 的计算。

### 5.4 扩散模型(Diffusion Models)
106. **DDPM 的前向加噪和反向去噪过程？** 噪声调度(Noise Schedule)。
107. **Score-based 和 DDPM 的联系？** Score Matching 的直觉理解。
108. **扩散模型 vs GAN vs VAE 的对比？** 训练稳定性、生成多样性、推理速度。
109. **Classifier-Free Guidance 的原理？** 为什么能提高生成质量？
110. **Latent Diffusion Model (Stable Diffusion) 的架构？** 在潜空间做扩散的优势。
111. **DDIM 如何加速采样？** 与 DDPM 采样的区别。
112. **ControlNet 如何实现可控生成？** 条件注入的方式。

### 5.5 自监督/对比学习
113. **SimCLR 的训练流程？** 数据增强策略的重要性。
114. **MoCo (Momentum Contrast) 的动量更新机制？** Queue 和 Momentum Encoder。
115. **BYOL 为什么不需要负样本？** Stop Gradient 的作用。
116. **MAE (Masked Autoencoder) 的原理？** 掩码率75%为什么有效？
117. **InfoNCE Loss 的推导和直觉理解。** 与对比学习的关系。
118. **DINO/DINOv2 的自监督训练策略？** Student-Teacher 框架。

---

## 六、计算机基础选择题

### 6.1 数据结构
119. 红黑树的五条性质？与AVL树的区别？
120. B树和B+树的区别？为什么数据库索引用B+树？
121. 哈希冲突的解决方法？开放定址法、链地址法、再哈希法。
122. 堆的插入和删除操作的时间复杂度？建堆的时间复杂度为什么是O(n)？
123. 图的存储方式？邻接矩阵 vs 邻接表的适用场景。
124. 跳表(Skip List)的原理和时间复杂度？Redis为什么用跳表？
125. 布隆过滤器(Bloom Filter)的原理？假阳性率如何控制？
126. 一致性哈希(Consistent Hashing)的原理和应用场景？

### 6.2 操作系统
127. 进程和线程的区别？协程(Coroutine)呢？
128. 进程间通信(IPC)的方式：管道、消息队列、共享内存、信号量、Socket。
129. 死锁产生的四个必要条件？如何预防和避免？银行家算法。
130. 虚拟内存的原理？页表和TLB的作用。
131. 页面置换算法：FIFO、LRU、OPT、Clock。LRU的实现方式。
132. 用户态和内核态的区别？系统调用的过程。
133. 文件系统：inode的概念？硬链接和软链接的区别。
134. CPU调度算法：FCFS、SJF、优先级调度、时间片轮转、多级反馈队列。
135. Linux 常用命令：`top`, `ps`, `netstat`, `grep`, `awk`, `sed`, `find`, `chmod`。
136. 什么是写时复制(Copy-on-Write, COW)？fork() 的实现原理。

### 6.3 计算机网络
137. OSI七层模型 vs TCP/IP四层模型的对应关系。
138. TCP 三次握手和四次挥手的详细过程？为什么不是两次/五次？
139. TCP 的拥塞控制机制？慢启动、拥塞避免、快重传、快恢复。
140. TCP 的滑动窗口机制？流量控制 vs 拥塞控制。
141. TCP 和 UDP 的区别？各自的适用场景。
142. TIME_WAIT 状态的作用？为什么是 2MSL？
143. HTTP 1.0 / 1.1 / 2.0 / 3.0 的区别？
144. HTTP 和 HTTPS 的区别？TLS 握手过程。
145. HTTP 常见状态码：200, 301, 302, 304, 400, 401, 403, 404, 500, 502, 503, 504。
146. Cookie、Session、Token 的区别？JWT 的结构。
147. DNS 的解析过程？递归查询 vs 迭代查询。
148. CDN 的工作原理？
149. 浏览器输入 URL 到页面展示的完整过程？（经典综合题）
150. 对称加密和非对称加密的区别？常见算法(AES, RSA)。
151. 什么是 CSRF？什么是 XSS？如何防御？

### 6.4 Linux/系统编程
152. select, poll, epoll 的区别？epoll 为什么高效？
153. 同步 vs 异步、阻塞 vs 非阻塞的区别？
154. Reactor 和 Proactor 模式的区别？
155. 共享内存(mmap)的使用场景和注意事项？

---

## 七、SQL高频题目

### 7.1 基础查询
156. **查询每个部门工资最高的员工。** (GROUP BY + 子查询/窗口函数)
157. **查询工资排名第N的员工。** (LIMIT OFFSET / DENSE_RANK)
158. **查询连续N天登录的用户。** (自连接 / 窗口函数 + 日期差)
159. **查询每个学生的平均成绩，且只显示平均分>80的。** (GROUP BY + HAVING)
160. **行转列和列转行。** (CASE WHEN / UNPIVOT)

### 7.2 窗口函数
161. **ROW_NUMBER(), RANK(), DENSE_RANK() 的区别和使用场景。**
162. **使用 LAG/LEAD 查询用户相邻两次登录的时间间隔。**
163. **使用 SUM() OVER() 计算累计销售额。**
164. **使用 NTILE() 将数据等分为N组。**

### 7.3 多表连接
165. **INNER JOIN, LEFT JOIN, RIGHT JOIN, FULL JOIN, CROSS JOIN 的区别。**
166. **查询没有购买记录的用户。** (LEFT JOIN + IS NULL / NOT EXISTS)
167. **自连接：查询同一部门中工资高于经理的员工。**
168. **三表以上的复合连接查询。**

### 7.4 经典LeetCode SQL题
| # | 题目 | 难度 |
|---|------|------|
| 175 | 组合两个表 | Easy |
| 176 | 第二高的薪水 | Medium |
| 177 | 第N高的薪水 | Medium |
| 178 | 分数排名 | Medium |
| 180 | 连续出现的数字 | Medium |
| 181 | 超过经理收入的员工 | Easy |
| 182 | 查找重复的电子邮箱 | Easy |
| 183 | 从不订购的客户 | Easy |
| 184 | 部门工资最高的员工 | Medium |
| 185 | 部门工资前三高的所有员工 | Hard |
| 196 | 删除重复的电子邮箱 | Easy |
| 197 | 上升的温度 | Easy |
| 262 | 行程和用户 | Hard |
| 550 | 游戏玩法分析 IV | Medium |
| 570 | 至少有5名直接下属的经理 | Medium |
| 584 | 寻找用户推荐人 | Easy |
| 595 | 大的国家 | Easy |
| 596 | 超过5名学生的课 | Easy |
| 601 | 体育馆的人流量 | Hard |
| 626 | 换座位 | Medium |
| 1141 | 查询近30天活跃用户数 | Easy |
| 1148 | 文章浏览 I | Easy |
| 1179 | 重新格式化部门表 | Easy |

---

## 八、经典智力题与逻辑推理题

### 8.1 经典逻辑题
169. **赛马问题：** 64匹马，8个赛道，至少几场比赛能找出最快的4匹？
170. **称球问题：** 12个球，1个次品(不知轻重)，用天平称3次找出来。
171. **倒水问题：** 3升和5升水壶，如何精确量出4升水？
172. **烧绳子问题：** 两根不均匀的绳子，每根烧完1小时，如何精确计时45分钟？
173. **海盗分金币：** 5个海盗分100金币，投票制，如何分配使利益最大化？
174. **两个鸡蛋问题：** 100层楼，2个鸡蛋，最少几次能找到临界楼层？（对应LeetCode 887）
175. **毒药问题：** 1000瓶药水中有1瓶毒药，10只老鼠，如何一次找出毒药？（二进制编码）
176. **囚犯帽子问题：** 100个囚犯戴黑白帽子站成一排，每人只能看到前面的人的帽子，从后往前猜，最多错几个？
177. **25匹马问题：** 25匹马，5个赛道，最少几场比赛能找出最快的3匹？（答案：7场）
178. **分金条问题：** 一根金条分成7段，每天给工人1段，只允许切2刀，如何做到？
179. **病狗问题：** N家人各养一条狗，有若干病狗，主人能看到别人的狗但看不到自己的，广场集合后第K天有人开枪，说明什么？

### 8.2 概率与博弈
180. **抛硬币游戏：** 两人轮流抛硬币，先抛出正面的人赢，先手胜率多少？（2/3）
181. **随机截断问题：** [0,1]区间上随机取两个点，三段能构成三角形的概率？（1/4）
182. **醉汉回家：** 一维随机游走，从原点出发，每步等概率+1或-1，回到原点的概率？（必然回到）
183. **信封问题(秘书问题)：** N个信封随机打乱，至少一个信封放对的概率？ $1 - 1/e$ (当N→∞)
184. **Monty Hall问题：** 三扇门，一扇后面有车，选定后主持人打开一扇空门，换门的胜率？(2/3)
185. **赌徒破产问题：** 初始资金a元，目标b元，每局赢概率p，最终破产概率是多少？
186. **扑克牌问题：** 52张牌随机抽5张，恰好是同花顺的概率？
187. **彩票问题（Coupon Collector）：** 集齐n种不同卡片，期望购买次数？ $n \cdot H_n$（调和级数）
188. **抽屉原理题：** 从1-100中取出多少个数，能保证有两个数的差恰好为10？

### 8.3 数学趣题
189. **火车过桥问题：** 火车长度L，桥长度B，速度V，完全通过桥需要多长时间？
190. **时钟问题：** 时针和分针一天中重合几次？（22次）
191. **递归问题：** 汉诺塔n个盘子至少需要多少步？（$2^n - 1$步）
192. **约瑟夫环问题：** n个人围成一圈，每次数到第k个人出局，最后留下的人是谁？

---

## 九、更多手撕代码题

### 9.1 深度学习高级组件

193. **手写 Batch Normalization (含训练/推理模式)**
```python
class BatchNorm:
    def __init__(self, num_features, eps=1e-5, momentum=0.1):
        self.gamma = np.ones(num_features)
        self.beta = np.zeros(num_features)
        self.running_mean = np.zeros(num_features)
        self.running_var = np.ones(num_features)
        self.eps = eps
        self.momentum = momentum
    
    def forward(self, x, training=True):
        if training:
            mean = np.mean(x, axis=0)
            var = np.var(x, axis=0)
            self.running_mean = (1 - self.momentum) * self.running_mean + self.momentum * mean
            self.running_var = (1 - self.momentum) * self.running_var + self.momentum * var
        else:
            mean = self.running_mean
            var = self.running_var
        x_norm = (x - mean) / np.sqrt(var + self.eps)
        return self.gamma * x_norm + self.beta
```

194. **手写 Dropout**
```python
def dropout(x, p=0.5, training=True):
    if not training:
        return x
    mask = (np.random.rand(*x.shape) > p).astype(float)
    return x * mask / (1 - p)  # inverted dropout
```

195. **手写 Group Normalization**
```python
def group_norm(x, num_groups, gamma, beta, eps=1e-5):
    N, C, H, W = x.shape
    x = x.reshape(N, num_groups, C // num_groups, H, W)
    mean = np.mean(x, axis=(2, 3, 4), keepdims=True)
    var = np.var(x, axis=(2, 3, 4), keepdims=True)
    x_norm = (x - mean) / np.sqrt(var + eps)
    x_norm = x_norm.reshape(N, C, H, W)
    return gamma * x_norm + beta
```

196. **手写 Focal Loss**
```python
def focal_loss(y_pred, y_true, alpha=0.25, gamma=2.0):
    eps = 1e-7
    y_pred = np.clip(y_pred, eps, 1 - eps)
    pt = np.where(y_true == 1, y_pred, 1 - y_pred)
    alpha_t = np.where(y_true == 1, alpha, 1 - alpha)
    loss = -alpha_t * (1 - pt) ** gamma * np.log(pt)
    return np.mean(loss)
```

197. **手写 GeLU 激活函数**
```python
def gelu(x):
    return 0.5 * x * (1 + np.tanh(np.sqrt(2/np.pi) * (x + 0.044715 * x**3)))
```

198. **手写 RoPE (旋转位置编码)**
```python
def rotary_embedding(x, seq_len, dim):
    """x: (batch, seq_len, dim)"""
    pos = np.arange(seq_len)[:, None]
    i = np.arange(0, dim, 2)[None, :]
    theta = pos / (10000 ** (i / dim))
    cos_theta = np.cos(theta)
    sin_theta = np.sin(theta)
    x1, x2 = x[..., ::2], x[..., 1::2]
    out1 = x1 * cos_theta - x2 * sin_theta
    out2 = x1 * sin_theta + x2 * cos_theta
    return np.stack([out1, out2], axis=-1).reshape(x.shape)
```

199. **手写 KV Cache 推理**
```python
class KVCacheAttention:
    def __init__(self, d_model):
        self.k_cache = []
        self.v_cache = []
    
    def forward(self, q, k, v):
        """增量推理：每次只输入新token的q,k,v"""
        self.k_cache.append(k)
        self.v_cache.append(v)
        K = np.concatenate(self.k_cache, axis=1)
        V = np.concatenate(self.v_cache, axis=1)
        d_k = q.shape[-1]
        scores = q @ K.transpose(0, 2, 1) / np.sqrt(d_k)
        weights = softmax(scores)
        return weights @ V
```

200. **手写 Beam Search**
```python
def beam_search(model, start_token, beam_width=3, max_len=50):
    sequences = [(start_token, 0.0)]  # (token_ids, log_prob)
    for _ in range(max_len):
        all_candidates = []
        for seq, score in sequences:
            if seq[-1] == EOS_TOKEN:
                all_candidates.append((seq, score))
                continue
            logits = model.predict(seq)
            log_probs = np.log(softmax(logits))
            top_k_idx = np.argsort(log_probs)[-beam_width:]
            for idx in top_k_idx:
                candidate = (seq + [idx], score + log_probs[idx])
                all_candidates.append(candidate)
        sequences = sorted(all_candidates, key=lambda x: x[1], reverse=True)[:beam_width]
        if all(s[-1] == EOS_TOKEN for s, _ in sequences):
            break
    return sequences[0]
```

### 9.2 经典算法实现

201. **手写 Dijkstra 最短路径**
```python
import heapq

def dijkstra(graph, start):
    dist = {node: float('inf') for node in graph}
    dist[start] = 0
    pq = [(0, start)]
    while pq:
        d, u = heapq.heappop(pq)
        if d > dist[u]: continue
        for v, w in graph[u]:
            if dist[u] + w < dist[v]:
                dist[v] = dist[u] + w
                heapq.heappush(pq, (dist[v], v))
    return dist
```

202. **手写 Trie (前缀树)**
```python
class TrieNode:
    def __init__(self):
        self.children = {}
        self.is_end = False

class Trie:
    def __init__(self):
        self.root = TrieNode()
    
    def insert(self, word):
        node = self.root
        for ch in word:
            if ch not in node.children:
                node.children[ch] = TrieNode()
            node = node.children[ch]
        node.is_end = True
    
    def search(self, word):
        node = self.root
        for ch in word:
            if ch not in node.children: return False
            node = node.children[ch]
        return node.is_end
    
    def starts_with(self, prefix):
        node = self.root
        for ch in prefix:
            if ch not in node.children: return False
            node = node.children[ch]
        return True
```

203. **手写拓扑排序 (Kahn算法 / BFS)**
```python
from collections import deque

def topological_sort(num_nodes, edges):
    in_degree = [0] * num_nodes
    adj = [[] for _ in range(num_nodes)]
    for u, v in edges:
        adj[u].append(v)
        in_degree[v] += 1
    
    queue = deque([i for i in range(num_nodes) if in_degree[i] == 0])
    order = []
    while queue:
        u = queue.popleft()
        order.append(u)
        for v in adj[u]:
            in_degree[v] -= 1
            if in_degree[v] == 0:
                queue.append(v)
    
    return order if len(order) == num_nodes else []  # 空=有环
```

204. **手写 KMP 字符串匹配**
```python
def kmp_search(text, pattern):
    def build_next(pattern):
        next_arr = [0] * len(pattern)
        j = 0
        for i in range(1, len(pattern)):
            while j > 0 and pattern[i] != pattern[j]:
                j = next_arr[j - 1]
            if pattern[i] == pattern[j]:
                j += 1
            next_arr[i] = j
        return next_arr
    
    next_arr = build_next(pattern)
    j = 0
    for i in range(len(text)):
        while j > 0 and text[i] != pattern[j]:
            j = next_arr[j - 1]
        if text[i] == pattern[j]:
            j += 1
        if j == len(pattern):
            return i - j + 1
    return -1
```

205. **手写蓄水池采样(Reservoir Sampling)**
```python
import random

def reservoir_sampling(stream, k):
    reservoir = []
    for i, item in enumerate(stream):
        if i < k:
            reservoir.append(item)
        else:
            j = random.randint(0, i)
            if j < k:
                reservoir[j] = item
    return reservoir
```

206. **手写最小堆**
```python
class MinHeap:
    def __init__(self):
        self.heap = []
    
    def push(self, val):
        self.heap.append(val)
        self._sift_up(len(self.heap) - 1)
    
    def pop(self):
        self.heap[0], self.heap[-1] = self.heap[-1], self.heap[0]
        val = self.heap.pop()
        if self.heap: self._sift_down(0)
        return val
    
    def _sift_up(self, i):
        while i > 0:
            parent = (i - 1) // 2
            if self.heap[i] < self.heap[parent]:
                self.heap[i], self.heap[parent] = self.heap[parent], self.heap[i]
                i = parent
            else: break
    
    def _sift_down(self, i):
        n = len(self.heap)
        while 2*i + 1 < n:
            child = 2*i + 1
            if child + 1 < n and self.heap[child+1] < self.heap[child]:
                child += 1
            if self.heap[i] > self.heap[child]:
                self.heap[i], self.heap[child] = self.heap[child], self.heap[i]
                i = child
            else: break
```

---

## 十、更多概率统计与数学题

### 10.1 进阶概率题
207. **Random(5) 生成 Random(7)：** 利用 Random(5) 等概率生成 1-7 的随机数。（拒绝采样）
208. **圆上均匀采样：** 如何在单位圆内均匀采样点？为什么不能简单用极坐标 r~U[0,1]？
209. **正态分布采样：** 用均匀分布生成正态分布（Box-Muller变换）。
210. **协方差矩阵的正定性：** 为什么协方差矩阵一定是半正定的？
211. **极大似然估计 vs 最大后验估计：** 对高斯分布参数的 MLE 推导。
212. **Dirichlet 分布：** 为什么是多项分布的共轭先验？
213. **Fisher信息矩阵的意义？** Cramér-Rao 下界。
214. **假设检验的 Type I Error 和 Type II Error 的区别？**
215. **p-value 的正确理解：** "p-value = 0.05" 到底意味着什么？

### 10.2 信息论
216. **信息熵(Shannon Entropy)的定义和直觉理解。** 为什么用 log？
217. **交叉熵和KL散度的关系？** $H(p,q) = H(p) + D_{KL}(p||q)$
218. **互信息(Mutual Information)的定义和应用。** 在特征选择中的作用。
219. **条件熵的定义？** $H(Y|X) = H(X,Y) - H(X)$

### 10.3 优化理论
220. **凸优化和非凸优化的区别？** 为什么深度学习是非凸优化但仍然有效？
221. **梯度下降、随机梯度下降、小批量梯度下降的区别。** 各自的优缺点。
222. **学习率太大/太小分别会导致什么问题？** 如何选择合适的学习率？
223. **动量(Momentum)的物理直觉？** 为什么能加速收敛？
224. **拉格朗日乘数法在 SVM 中的应用。** KKT 条件的含义。
225. **牛顿法和梯度下降法的区别？** 为什么牛顿法收敛更快但不常用于深度学习？

### 10.4 矩阵/线性代数进阶
226. **矩阵乘法的时间复杂度？** Strassen 算法的改进。
227. **低秩矩阵分解(Low-Rank Factorization)的应用？** LoRA 中的原理。
228. **Attention矩阵为什么是低秩的？** 对 Linear Attention 的启示。
229. **正交矩阵的性质和在深度学习中的应用？** 正交初始化。
230. **Jacobian 矩阵和 Hessian 矩阵的区别和意义？**

---

## 附录：各方向必刷题数量建议

| 方向 | 编程题 | ML/DL理论 | 专业方向 | 手撕代码 | 总计 |
|------|--------|-----------|----------|----------|------|
| **通用算法岗** | 150+ | 60+ | 30+ | 20+ | 260+ |
| **推荐/广告** | 100+ | 60+ | 60+ | 15+ | 235+ |
| **CV** | 100+ | 40+ | 50+ | 25+ | 215+ |
| **NLP/大模型** | 100+ | 50+ | 60+ | 20+ | 230+ |
| **多模态** | 100+ | 50+ | 40+ | 20+ | 210+ |

> **总结：** 本补充篇新增 250+ 道题目，加上原有的 149+ 题，总计收录 **400+ 道题目**。覆盖算法岗笔试/面试的各个维度。
