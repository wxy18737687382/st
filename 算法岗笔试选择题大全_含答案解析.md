# 算法岗笔试选择题大全（含答案解析）

> 共 **200+ 道选择题**，覆盖：机器学习、深度学习、大模型/Transformer、概率统计、数据结构、操作系统、计算机网络、数据库/SQL
> 每题均附 **答案 + 解析**

---

## 目录
- [一、机器学习选择题 (40题)](#一机器学习选择题)
- [二、深度学习选择题 (40题)](#二深度学习选择题)
- [三、大模型/Transformer选择题 (30题)](#三大模型transformer选择题)
- [四、概率论与数理统计选择题 (30题)](#四概率论与数理统计选择题)
- [五、数据结构与算法选择题 (30题)](#五数据结构与算法选择题)
- [六、操作系统选择题 (20题)](#六操作系统选择题)
- [七、计算机网络选择题 (20题)](#七计算机网络选择题)
- [八、数据库/SQL选择题 (15题)](#八数据库sql选择题)

---

## 一、机器学习选择题

**1. 以下哪种算法属于判别式模型（Discriminative Model）？**
A. 隐马尔可夫模型 (HMM)
B. 朴素贝叶斯 (Naive Bayes)
C. 支持向量机 (SVM)
D. 高斯混合模型 (GMM)

> **答案：C**
> **解析：** 判别模型直接建模 $P(y|x)$，如 SVM、逻辑回归、神经网络；生成模型建模联合分布 $P(x,y)$ 或 $P(x|y)P(y)$，如朴素贝叶斯、HMM、GMM。

---

**2. 关于L1正则化和L2正则化，以下说法正确的是？**
A. L1正则化倾向于产生稀疏解，L2正则化不会
B. L2正则化倾向于产生稀疏解，L1正则化不会
C. L1和L2都会产生稀疏解
D. L1和L2都不会产生稀疏解

> **答案：A**
> **解析：** L1正则化在优化过程中倾向于将某些参数压缩为0，产生稀疏解（特征选择）；L2正则化倾向于让参数值较小但不为0。从贝叶斯角度看，L1等价于拉普拉斯先验，L2等价于高斯先验。

---

**3. 以下关于偏差(Bias)和方差(Variance)的描述，错误的是？**
A. 偏差衡量模型预测值与真实值之间的差距
B. 方差衡量模型在不同训练集上预测值的波动程度
C. 高偏差模型通常是过拟合
D. 高方差模型通常是过拟合

> **答案：C**
> **解析：** 高偏差对应欠拟合（模型过于简单），高方差对应过拟合（模型过于复杂）。

---

**4. 逻辑回归(Logistic Regression)的损失函数使用交叉熵而不使用MSE，主要原因是？**
A. 交叉熵计算速度更快
B. 使用MSE时损失函数为非凸函数，存在局部最优
C. MSE不能用于分类任务
D. 交叉熵的正则化效果更好

> **答案：B**
> **解析：** 逻辑回归使用Sigmoid函数，若用MSE作为损失函数，得到的是非凸函数，梯度下降可能陷入局部最优；使用交叉熵则能保证是凸函数，收敛到全局最优。

---

**5. 在SVM中，增大惩罚参数C的效果是？**
A. 增大间隔，允许更多误分类
B. 减小间隔，减少误分类
C. 对模型没有影响
D. 增大间隔，减少误分类

> **答案：B**
> **解析：** C 越大，对误分类的惩罚越重，模型倾向于减少训练集上的错误（间隔更小）；C越小则间隔越大，允许更多误分类，泛化能力可能更好。

---

**6. 随机森林(Random Forest)和GBDT的主要区别，以下描述正确的是？**
A. 随机森林属于Boosting，GBDT属于Bagging
B. 随机森林的基学习器之间相互独立，GBDT的基学习器有序列依赖
C. GBDT可以并行训练，随机森林只能串行训练
D. 随机森林和GBDT都只能用决策树作为基学习器

> **答案：B**
> **解析：** 随机森林属于Bagging，各棵树独立并行训练；GBDT属于Boosting，后一棵树拟合前面所有树的残差，是串行训练的。

---

**7. XGBoost相比GBDT的改进，不包括以下哪一项？**
A. 使用了二阶泰勒展开
B. 加入了正则化项
C. 支持特征并行
D. 使用了注意力机制

> **答案：D**
> **解析：** XGBoost的核心改进包括：二阶泰勒展开近似目标函数、正则化防止过拟合、支持列采样和特征并行、内置缺失值处理等。注意力机制是Transformer的组件。

---

**8. ROC曲线的横轴和纵轴分别是？**
A. 横轴Precision，纵轴Recall
B. 横轴FPR(假阳性率)，纵轴TPR(真阳性率)
C. 横轴Recall，纵轴Precision
D. 横轴TPR，纵轴FPR

> **答案：B**
> **解析：** ROC曲线的横轴是FPR = FP/(FP+TN)，纵轴是TPR = TP/(TP+FN)。AUC是ROC曲线下的面积。

---

**9. 当正负样本极度不平衡时（如1:1000），以下哪个评价指标最不可靠？**
A. AUC
B. 准确率(Accuracy)
C. F1-Score
D. PR曲线下面积

> **答案：B**
> **解析：** 在极度不平衡时，即使模型将所有样本预测为负类，准确率也可达到99.9%，因此Accuracy不可靠。应使用AUC、F1、PR-AUC等。

---

**10. K-Means算法对初始聚类中心敏感，以下哪种方法可以缓解这个问题？**
A. 增加K值
B. 使用K-Means++初始化
C. 减少迭代次数
D. 使用Manhattan距离代替欧氏距离

> **答案：B**
> **解析：** K-Means++通过选择尽量分散的初始中心点来改善初始化问题，使算法更稳定、收敛更快。

---

**11. PCA降维的目标是？**
A. 最小化投影后的方差
B. 最大化投影后的方差
C. 最小化数据点之间的距离
D. 最大化数据点之间的距离

> **答案：B**
> **解析：** PCA的目标是找到使投影后数据方差最大的方向（即主成分），从而保留最多的信息。

---

**12. 以下关于决策树的说法，错误的是？**
A. ID3使用信息增益作为特征选择准则
B. C4.5使用信息增益比，解决了ID3偏向选择取值多的特征的问题
C. CART用于分类时使用基尼指数，用于回归时使用MSE
D. 决策树天然支持处理缺失值，不需要额外处理

> **答案：D**
> **解析：** 标准的决策树算法（如ID3）不支持缺失值处理。C4.5和CART对缺失值有不同的处理策略（如代理分裂），但不能说"天然支持"且"不需要额外处理"。

---

**13. 以下关于集成学习的描述，正确的是？**
A. Bagging主要降低偏差
B. Boosting主要降低方差
C. Bagging主要降低方差
D. 随机森林中的决策树越多，越容易过拟合

> **答案：C**
> **解析：** Bagging通过并行训练多个模型并取平均/投票来降低方差；Boosting通过串行纠错来降低偏差。随机森林中树越多通常不会过拟合（只会趋于收敛）。

---

**14. 特征归一化（如Min-Max Scaling）对以下哪个模型影响最大？**
A. 决策树
B. KNN
C. 随机森林
D. 朴素贝叶斯

> **答案：B**
> **解析：** KNN基于距离计算，对特征尺度敏感，必须做归一化。决策树、随机森林基于特征分裂，不受尺度影响。

---

**15. 梯度下降法中，学习率过大可能导致什么问题？**
A. 收敛速度太慢
B. 陷入局部最优
C. 震荡不收敛或发散
D. 过拟合

> **答案：C**
> **解析：** 学习率过大会导致每次更新步长太大，在最优解附近来回震荡甚至发散；学习率过小则收敛速度太慢。

---

**16. EM算法的E步做的是？**
A. 更新模型参数
B. 计算隐变量的后验分布（期望）
C. 计算损失函数
D. 进行梯度下降

> **答案：B**
> **解析：** E步(Expectation)：固定参数，计算隐变量的后验概率（期望）。M步(Maximization)：固定隐变量期望，更新参数使似然函数最大化。

---

**17. 以下哪种方法不能用于特征选择？**
A. Lasso回归
B. 卡方检验
C. PCA
D. 互信息

> **答案：C**
> **解析：** PCA是特征降维/特征提取，它将原始特征变换为新的特征（主成分），不是从原始特征中选择子集。Lasso（L1正则化）、卡方检验、互信息都可以用于特征选择。

---

**18. 关于SVM中的核函数，以下描述错误的是？**
A. 核函数可以将低维空间映射到高维空间
B. 多项式核和RBF核都是常见的核函数
C. 核技巧(Kernel Trick)避免了显式计算高维空间中的坐标
D. 核函数可以是任意函数

> **答案：D**
> **解析：** 核函数必须满足Mercer条件（半正定），不是任意函数都可以作为核函数。

---

**19. LightGBM相比XGBoost的主要改进是？**
A. 使用了三阶泰勒展开
B. 使用直方图算法(Histogram-based)加速训练和按叶子(leaf-wise)生长策略
C. 使用注意力机制
D. 使用了不同的损失函数

> **答案：B**
> **解析：** LightGBM的核心改进包括：直方图算法（离散化特征值减少计算）、leaf-wise生长（vs XGBoost的level-wise）、GOSS和EFB等。

---

**20. 交叉验证(Cross-Validation)的主要目的是？**
A. 增加训练数据
B. 评估模型的泛化能力，减少评估偏差
C. 加速模型训练
D. 选择最优的特征

> **答案：B**
> **解析：** 交叉验证通过多次划分训练/验证集并取平均来更准确地评估模型的泛化性能。

---

**21. 以下关于AUC指标的描述，正确的是？**
A. AUC值越大，模型越差
B. AUC = 0.5 表示模型效果等同于随机猜测
C. AUC的取值范围是[0, +∞)
D. AUC不适用于二分类任务

> **答案：B**
> **解析：** AUC取值范围是[0,1]。AUC=0.5时等同于随机分类器；AUC=1时为完美分类。AUC越大越好。

---

**22. 关于逻辑回归和朴素贝叶斯的关系，以下说法正确的是？**
A. 两者完全无关
B. 在特定假设下（特征独立+高斯分布），两者的决策边界可以是相同的
C. 朴素贝叶斯一定比逻辑回归效果好
D. 逻辑回归是生成模型，朴素贝叶斯是判别模型

> **答案：B**
> **解析：** 当朴素贝叶斯的条件独立假设成立且类条件分布为高斯分布时，其决策面与LR等价。LR是判别模型，朴素贝叶斯是生成模型。

---

**23. 过拟合的常见解决方法，不包括以下哪项？**
A. 增大训练集
B. 减小模型复杂度
C. 增大学习率
D. 使用正则化

> **答案：C**
> **解析：** 增大学习率可能导致训练不稳定甚至发散，不是解决过拟合的方法。常见方法包括：增大数据集、正则化、降低模型复杂度、Dropout、早停等。

---

**24. 关于GBDT中的残差，以下描述正确的是？**
A. GBDT的每棵树拟合的是前一棵树的输出
B. GBDT的每棵树拟合的是损失函数的负梯度
C. GBDT的每棵树拟合的是数据的原始标签
D. GBDT的每棵树使用随机子集训练

> **答案：B**
> **解析：** GBDT中每棵新树拟合的是当前损失函数相对于模型输出的负梯度（伪残差），当损失函数为MSE时等价于拟合残差。

---

**25. 对于多分类问题，Softmax函数的作用是？**
A. 将输入映射到(0,1)区间
B. 将输入向量转化为概率分布，各元素之和为1
C. 对输入进行归一化，使均值为0方差为1
D. 将负数映射为0

> **答案：B**
> **解析：** Softmax将K维向量映射为一个概率分布，每个元素在(0,1)之间且总和为1。

---

**26. 目标编码(Target Encoding)的主要风险是？**
A. 增加模型训练时间
B. 导致信息泄露(Data Leakage)
C. 不适用于类别特征
D. 会产生缺失值

> **答案：B**
> **解析：** Target Encoding使用目标变量信息对特征编码，容易在训练集上过拟合（信息泄露）。通常需要结合交叉验证来缓解。

---

**27. 关于F1-Score，以下描述正确的是？**
A. F1 = Precision × Recall
B. F1 = 2 × Precision × Recall / (Precision + Recall)
C. F1 = (Precision + Recall) / 2
D. F1 = Precision / Recall

> **答案：B**
> **解析：** F1是Precision和Recall的调和平均值，公式为 F1 = 2PR/(P+R)。

---

**28. SMOTE算法解决类别不平衡的原理是？**
A. 对多数类进行欠采样
B. 在少数类样本的特征空间中进行插值生成新样本
C. 调整损失函数的权重
D. 使用GAN生成少数类样本

> **答案：B**
> **解析：** SMOTE通过在少数类样本与其近邻之间进行线性插值来合成新样本，从而增加少数类的数量。

---

**29. 以下关于信息增益的描述，正确的是？**
A. 信息增益 = 条件熵 - 信息熵
B. 信息增益 = 信息熵 - 条件熵
C. 信息增益越大，特征的区分能力越弱
D. 信息增益的值可以为负

> **答案：B**
> **解析：** 信息增益 = H(Y) - H(Y|X)，表示知道特征X后对Y不确定性的减少量。信息增益越大，特征区分能力越强。信息增益 ≥ 0。

---

**30. KNN中K值选择对模型的影响，以下描述正确的是？**
A. K值越大，模型越复杂，越容易过拟合
B. K值越小，模型越简单，越容易欠拟合
C. K值越大，模型越简单（平滑），决策边界越平滑
D. K值对模型没有影响

> **答案：C**
> **解析：** K越小，模型越复杂（容易过拟合，对噪声敏感）；K越大，模型越简单（决策边界越平滑，可能欠拟合）。

---

**31. 以下哪种损失函数对异常值（Outliers）最鲁棒？**
A. MSE (均方误差)
B. MAE (平均绝对误差)
C. Huber Loss
D. B和C都比A更鲁棒

> **答案：D**
> **解析：** MSE对异常值敏感（误差被平方放大）。MAE使用绝对值，对异常值鲁棒。Huber Loss结合了MSE和MAE的优点，在小误差时用MSE、大误差时用MAE。

---

**32. 以下关于GMM(高斯混合模型)的描述，错误的是？**
A. GMM是一种软聚类方法
B. GMM使用EM算法求解参数
C. GMM假设数据由多个高斯分布混合生成
D. GMM只能用于分类任务

> **答案：D**
> **解析：** GMM是一种聚类（无监督学习）方法，也可用于密度估计等任务。它通过EM算法迭代估计各高斯分量的参数。

---

**33. 关于方差和偏差的分解，以下哪个对应"模型在不同训练集上的不一致性"？**
A. 偏差(Bias)
B. 方差(Variance)
C. 噪声(Noise)
D. 以上都不是

> **答案：B**
> **解析：** 方差衡量模型在不同训练集上预测结果的波动程度。偏差衡量模型的系统性偏离。噪声是数据本身的不可约误差。

---

**34. Ridge回归（L2正则化线性回归）的解的形式为？**
A. $w = (X^TX)^{-1}X^Ty$
B. $w = (X^TX + \lambda I)^{-1}X^Ty$
C. $w = (X^TX - \lambda I)^{-1}X^Ty$
D. $w = X^T(XX^T + \lambda I)^{-1}y$

> **答案：B**
> **解析：** Ridge回归在最小二乘法的正规方程基础上加了正则项λI，使得即使X^TX不可逆也能求解。

---

**35. 以下关于朴素贝叶斯的"朴素"假设，正确的是？**
A. 假设各类的先验概率相等
B. 假设特征之间条件独立
C. 假设数据服从正态分布
D. 假设所有特征的重要性相同

> **答案：B**
> **解析：** "朴素"指假设在给定类别下，各特征之间相互条件独立。即 $P(x_1,x_2,...|y) = \prod P(x_i|y)$。

---

**36. 核函数$K(x_i, x_j) = (\langle x_i, x_j \rangle + 1)^d$ 属于？**
A. 线性核
B. 多项式核
C. RBF(径向基)核
D. Sigmoid核

> **答案：B**
> **解析：** 多项式核的一般形式为 $K(x,y) = (\gamma \langle x,y \rangle + r)^d$，这里 $\gamma=1, r=1$。

---

**37. 以下关于Precision和Recall的说法，错误的是？**
A. Precision = TP / (TP + FP)
B. Recall = TP / (TP + FN)
C. 提高阈值通常会提高Precision、降低Recall
D. Precision和Recall之间没有关系

> **答案：D**
> **解析：** Precision和Recall通常存在trade-off关系，提高一个往往会降低另一个。

---

**38. 以下哪个不是降维方法？**
A. PCA
B. t-SNE
C. LDA (线性判别分析)
D. SMOTE

> **答案：D**
> **解析：** SMOTE是过采样方法（处理类别不平衡），不是降维方法。PCA、t-SNE、LDA都可以用于降维。

---

**39. 关于XGBoost中的列采样(Column Subsampling)，其作用是？**
A. 减少计算量并防止过拟合
B. 加速数据读取
C. 增加特征维度
D. 替代正则化

> **答案：A**
> **解析：** 列采样（类似随机森林的特征采样）可以减少每棵树考虑的特征数量，降低计算量的同时增加模型多样性，防止过拟合。

---

**40. 在不平衡分类中，以下哪种方法属于代价敏感学习(Cost-Sensitive Learning)？**
A. SMOTE
B. 欠采样
C. 对不同类别赋予不同的损失权重
D. 数据增强

> **答案：C**
> **解析：** 代价敏感学习通过对少数类赋予更高的错分代价（损失权重），使模型更关注少数类的分类效果。

---

## 二、深度学习选择题

**41. 在深度神经网络中使用ReLU激活函数的主要优点是？**
A. 将数据归一化到[0,1]之间
B. 能够缓解梯度消失问题，且计算速度快
C. 解决了非线性映射能力不足的问题
D. 在反向传播中梯度永远不会为0

> **答案：B**
> **解析：** ReLU在x>0时导数恒为1，计算简单，能有效缓解深层网络的梯度消失。但x<0时梯度为0（dead ReLU问题），所以D选项错误。

---

**42. 输入图像尺寸为 $224 \times 224 \times 3$，使用5个 $7 \times 7$ 卷积核，stride=1，padding=0，输出特征图维度是？**
A. $218 \times 218 \times 3$
B. $218 \times 218 \times 5$
C. $220 \times 220 \times 5$
D. $217 \times 217 \times 3$

> **答案：B**
> **解析：** 输出尺寸 = (224 - 7 + 0) / 1 + 1 = 218。通道数由卷积核数量决定 = 5。

---

**43. 若一个卷积层输入通道 $C_{in}=64$，输出通道 $C_{out}=128$，卷积核 $3 \times 3$，含偏置，参数量为？**
A. 73,728
B. 73,856
C. 36,864
D. 36,992

> **答案：B**
> **解析：** 参数量 = $C_{in} \times C_{out} \times K \times K + C_{out}$ = $64 \times 128 \times 3 \times 3 + 128$ = $73728 + 128$ = $73856$。

---

**44. 关于Batch Normalization（BN），以下描述错误的是？**
A. BN可以加快模型的收敛速度
B. BN在训练阶段和测试阶段的操作完全相同
C. BN能缓解梯度消失或爆炸问题
D. BN通过规范化每层的输入分布使其保持稳定

> **答案：B**
> **解析：** BN在训练时使用当前Batch的均值和方差，推理时使用训练过程中累积的移动平均(Running Mean/Var)，两者不同。

---

**45. 以下关于Dropout的说法，正确的是？**
A. Dropout在训练和推理时都随机丢弃神经元
B. Dropout只在训练时随机丢弃，推理时使用全部神经元（并缩放）
C. Dropout的丢弃率越高，模型效果越好
D. Dropout只能用于全连接层

> **答案：B**
> **解析：** Dropout在训练时随机丢弃部分神经元，推理时保留全部。Inverted Dropout在训练时除以(1-p)来缩放，推理时无需额外操作。

---

**46. 以下关于残差网络(ResNet)的说法，错误的是？**
A. 残差连接使得网络可以训练更深的层数
B. 残差块学习的是输入到输出的残差映射 $F(x) = H(x) - x$
C. 残差连接解决了深层网络的退化(Degradation)问题
D. 残差连接消除了梯度消失问题

> **答案：D**
> **解析：** 残差连接缓解了梯度消失问题（因为梯度可以通过shortcut直接回传），但并没有完全消除。

---

**47. 以下激活函数中，输出范围为(-1, 1)的是？**
A. ReLU
B. Sigmoid
C. Tanh
D. Softmax

> **答案：C**
> **解析：** ReLU输出[0, +∞)，Sigmoid输出(0, 1)，Tanh输出(-1, 1)，Softmax输出(0, 1)且和为1。

---

**48. Adam优化器结合了以下哪两种方法的优点？**
A. SGD + L2正则化
B. Momentum + RMSProp
C. Adagrad + BatchNorm
D. SGD + Dropout

> **答案：B**
> **解析：** Adam = Adaptive Moment Estimation，结合了Momentum（一阶动量）和RMSProp（二阶动量/自适应学习率）。

---

**49. 关于梯度消失和梯度爆炸，以下说法正确的是？**
A. 梯度消失只发生在RNN中
B. 使用Sigmoid激活函数的深层网络容易梯度消失
C. 梯度爆炸可以通过增大学习率来解决
D. BatchNorm会导致梯度爆炸

> **答案：B**
> **解析：** Sigmoid导数最大值为0.25，多层连乘后梯度趋近于0（梯度消失）。梯度爆炸可通过梯度裁剪(Gradient Clipping)来缓解。

---

**50. 1×1卷积的作用不包括以下哪一项？**
A. 改变通道数（升维/降维）
B. 增加非线性
C. 跨通道信息整合
D. 扩大感受野

> **答案：D**
> **解析：** 1×1卷积不改变空间维度，因此不会扩大感受野。它的作用是改变通道数、增加非线性（配合激活函数）、进行跨通道信息融合。

---

**51. 以下关于池化层(Pooling)的说法，错误的是？**
A. 池化层可以减少特征图的空间尺寸
B. 池化层有可学习的参数
C. 最大池化提取区域内的最显著特征
D. 池化可以提供一定的平移不变性

> **答案：B**
> **解析：** 标准的池化层（Max/Average Pooling）没有可学习的参数，只是一种固定的下采样操作。

---

**52. Xavier初始化适用于哪种激活函数？**
A. ReLU
B. Sigmoid/Tanh
C. LeakyReLU
D. 任意激活函数

> **答案：B**
> **解析：** Xavier初始化假设激活函数在0附近是线性的，适合Sigmoid/Tanh。Kaiming(He)初始化适合ReLU系列。

---

**53. 在训练深度学习模型时，为缓解过拟合，以下哪些方法有效？（多选）**
A. 使用Dropout
B. 增加网络层数
C. 使用Batch Normalization
D. 数据增强(Data Augmentation)

> **答案：ACD**
> **解析：** 增加网络层数(B)会增加模型复杂度，可能加剧过拟合。Dropout、BN（有一定正则化效果）、数据增强都可以缓解过拟合。

---

**54. 关于转置卷积(Transposed Convolution)，以下描述正确的是？**
A. 转置卷积是卷积的逆操作
B. 转置卷积可以用于上采样，增大特征图尺寸
C. 转置卷积不会产生棋盘格效应(Checkerboard Artifacts)
D. 转置卷积没有可学习参数

> **答案：B**
> **解析：** 转置卷积不是卷积的逆操作（不能恢复原始值），而是用于上采样。它可能产生棋盘格效应（当stride和kernel size不匹配时）。

---

**55. 关于GeLU激活函数，以下描述正确的是？**
A. GeLU是分段线性函数
B. GeLU = x × Φ(x)，其中Φ是标准正态分布的CDF
C. GeLU与ReLU完全等价
D. GeLU不可导

> **答案：B**
> **解析：** GeLU = $x \cdot \Phi(x)$，是光滑的非线性函数，在BERT、GPT等模型中广泛使用。它是处处可导的。

---

**56. LSTM中，遗忘门(Forget Gate)的作用是？**
A. 决定输出什么信息
B. 决定丢弃记忆单元中的哪些信息
C. 决定接收哪些新信息
D. 控制学习率大小

> **答案：B**
> **解析：** 遗忘门通过Sigmoid函数输出0-1之间的值，控制上一时刻记忆单元中哪些信息被保留、哪些被丢弃。

---

**57. 深度可分离卷积(Depthwise Separable Convolution)相比标准卷积的参数量比例约为？**
A. $\frac{1}{C_{out}} + \frac{1}{K^2}$
B. $\frac{1}{C_{in}} + \frac{1}{K^2}$
C. $C_{in} \times K^2$
D. $\frac{K^2}{C_{out} \times K^2} = \frac{1}{C_{out}}$

> **答案：A**
> **解析：** 标准卷积参数量 = $C_{in} \times C_{out} \times K^2$；深度可分离卷积 = $C_{in} \times K^2 + C_{in} \times C_{out}$。比例 ≈ $1/C_{out} + 1/K^2$。

---

**58. 关于Warmup学习率策略，以下描述正确的是？**
A. Warmup在训练开始时使用较大的学习率
B. Warmup在训练开始时从一个很小的学习率逐渐增大到设定值
C. Warmup只适用于SGD优化器
D. Warmup会导致模型训练更慢

> **答案：B**
> **解析：** Warmup在训练初期用较小的学习率，逐步升到预设值，避免训练初期参数不稳定导致的剧烈波动。

---

**59. 关于标签平滑(Label Smoothing)，以下描述正确的是？**
A. 将hard label(0/1)替换为soft label(如0.1/0.9)
B. 标签平滑会增加过拟合
C. 标签平滑不影响模型的泛化能力
D. 标签平滑只能用于回归任务

> **答案：A**
> **解析：** Label Smoothing将one-hot标签替换为软标签（如将1替换为0.9，0替换为0.1/K），防止模型过度自信，起正则化作用，提升泛化能力。

---

**60. 以下关于反向传播的说法，正确的是？**
A. 反向传播不使用链式法则
B. 反向传播从输出层向输入层方向计算梯度
C. 反向传播只适用于卷积网络
D. 反向传播可以直接求出全局最优解

> **答案：B**
> **解析：** 反向传播基于链式法则，从输出层开始逐层向输入层方向计算损失函数对各参数的梯度。它适用于任何可微分的网络结构。

---

**61. 关于知识蒸馏(Knowledge Distillation)，以下描述正确的是？**
A. 教师模型(Teacher)通常比学生模型(Student)更小
B. 学生模型通过学习教师模型的软标签(Soft Labels)来提升性能
C. 蒸馏中的温度参数T越大，输出概率分布越尖锐
D. 知识蒸馏只能用于分类任务

> **答案：B**
> **解析：** Teacher模型更大更强，Student学习Teacher的软标签（经过温度缩放的softmax输出）。温度T越大，概率分布越平滑（越接近均匀分布）。

---

**62. 以下关于LayerNorm和BatchNorm的区别，正确的是？**
A. BatchNorm在batch维度归一化，LayerNorm在特征维度归一化
B. BatchNorm适用于NLP任务，LayerNorm适用于CV任务
C. 两者的数学公式完全不同
D. LayerNorm依赖于batch size

> **答案：A**
> **解析：** BN在batch维度（跨样本）计算均值方差，依赖batch size；LN在特征维度（每个样本内部）归一化，不依赖batch size，更适合NLP和Transformer。

---

**63. 模型参数量为7B（70亿）的大模型，使用FP16精度存储，大约需要多少GPU显存？**
A. 7GB
B. 14GB
C. 28GB
D. 56GB

> **答案：B**
> **解析：** FP16每个参数占2字节，7B × 2 bytes = 14GB。若使用FP32则需28GB，INT8则需7GB，INT4则需约3.5GB。

---

**64. 梯度裁剪(Gradient Clipping)的作用是？**
A. 加速模型收敛
B. 防止梯度爆炸
C. 防止梯度消失
D. 减少模型参数量

> **答案：B**
> **解析：** 梯度裁剪将梯度范数限制在某个阈值内，防止梯度过大（爆炸）导致训练不稳定。

---

**65. 关于Focal Loss，以下描述正确的是？**
A. Focal Loss用于解决类别不平衡问题中简单样本主导训练的问题
B. Focal Loss比交叉熵损失的值更大
C. Focal Loss中的γ参数越大，对简单样本的抑制越弱
D. Focal Loss只用于语义分割

> **答案：A**
> **解析：** Focal Loss = $-\alpha(1-p_t)^\gamma \log(p_t)$，通过 $(1-p_t)^\gamma$ 因子降低易分类样本的权重，使模型更关注难分类样本。γ越大，抑制越强。

---

**66-80.（更多深度学习选择题）**

**66. 以下哪种方法不属于数据增强？**
A. 随机裁剪 B. Mixup C. 权重衰减(Weight Decay) D. CutMix
> **答案：C** （Weight Decay是正则化方法）

**67. AdamW相比Adam的改进是？**
A. 使用三阶矩 B. 解耦了权重衰减和自适应学习率 C. 不需要学习率 D. 使用余弦退火
> **答案：B** （Adam中L2正则化与自适应学习率耦合，AdamW将它们解耦）

**68. 感受野(Receptive Field)越大，网络捕获的特征越？**
A. 局部 B. 全局 C. 精细 D. 以上都不对
> **答案：B** （大感受野捕获全局/大范围特征）

**69. 以下关于空洞卷积(Dilated Convolution)的描述，正确的是？**
A. 增加参数量来扩大感受野 B. 不增加参数量即可扩大感受野 C. 减小感受野 D. 等价于标准卷积
> **答案：B** （空洞卷积通过dilation rate在卷积核元素间插入间隔，不增加参数）

**70. Cosine Annealing学习率调度的特点是？**
A. 学习率线性下降 B. 学习率按余弦函数周期性变化 C. 学习率保持不变 D. 学习率指数下降
> **答案：B**

---

## 三、大模型/Transformer选择题

**71. Transformer的自注意力机制中，缩放因子 $1/\sqrt{d_k}$ 的主要目的是？**
A. 减少计算量
B. 防止点积过大导致Softmax后梯度消失
C. 增强模型的表达能力
D. 强制模型关注特定位置

> **答案：B**
> **解析：** 当维度 $d_k$ 较大时，Q和K的点积方差变大，导致Softmax进入饱和区，梯度极小。除以 $\sqrt{d_k}$ 可以稳定梯度。

---

**72. 多头注意力(Multi-Head Attention)设置多个头的主要目的是？**
A. 减少计算复杂度
B. 允许模型在不同子空间中并行学习和捕获信息
C. 增加参数量提升记忆能力
D. 解决梯度消失问题

> **答案：B**
> **解析：** 多头注意力类似CNN中的多通道，在不同的表示子空间中学习不同的注意力模式。

---

**73. 关于KV Cache，以下描述正确的是？**
A. KV Cache主要用于加速训练
B. KV Cache缓存历史Token的Key和Value，避免自回归推理时的重复计算
C. KV Cache的显存不随序列长度增长
D. KV Cache会增加单次推理的计算量

> **答案：B**
> **解析：** KV Cache在推理时缓存已生成Token的K和V，每生成新Token只需计算当前Token的Q。显存随序列长度线性增长。

---

**74. 关于LoRA微调，以下说法正确的是？**
A. LoRA更新模型的所有参数
B. LoRA在预训练权重旁添加可训练的低秩矩阵来近似全参更新
C. LoRA只适用于小规模模型
D. LoRA微调后推理速度会变慢

> **答案：B**
> **解析：** LoRA冻结预训练权重，只训练低秩适配器矩阵(A, B)，$\Delta W = BA$。推理时可将适配器权重合并到原始权重中，不增加推理延迟。

---

**75. 关于位置编码，以下说法错误的是？**
A. Transformer本身不具备处理序列顺序的能力
B. RoPE将位置信息通过旋转矩阵作用于Q和K向量
C. Sinusoidal位置编码是可学习的
D. ALiBi通过在注意力分数上添加线性偏置来引入位置信息

> **答案：C**
> **解析：** Sinusoidal位置编码是固定的（基于正弦余弦函数），不可学习。可学习的位置编码是另一种方案。

---

**76. Encoder-Only架构（如BERT）最适合以下哪类任务？**
A. 文本生成
B. 机器翻译
C. 文本分类和序列标注
D. 对话系统

> **答案：C**
> **解析：** Encoder-Only可以看到完整上下文（双向），适合理解类任务（分类、NER等）。Decoder-Only适合生成，Encoder-Decoder适合seq2seq（翻译、摘要）。

---

**77. BERT的预训练任务包括？**
A. Causal Language Modeling (CLM)
B. Masked Language Modeling (MLM) + Next Sentence Prediction (NSP)
C. 对比学习
D. 强化学习

> **答案：B**
> **解析：** BERT使用MLM（随机遮掩15%的token并预测）和NSP（判断两个句子是否连续）进行预训练。

---

**78. 在大模型中，Pre-Norm（归一化在子层之前）相比Post-Norm的优势是？**
A. 模型效果更好
B. 训练更稳定，更容易训练深层网络
C. 推理速度更快
D. 参数量更少

> **答案：B**
> **解析：** Pre-Norm将归一化放在残差分支的入口，使梯度流更平稳，训练更稳定。Post-Norm理论上收敛后效果可能更好但训练更不稳定。

---

**79. Flash Attention的核心优化思想是？**
A. 使用更少的注意力头
B. 减少GPU HBM与SRAM之间的IO次数，通过分块(Tiling)计算注意力
C. 使用近似注意力替代精确注意力
D. 减少模型参数量

> **答案：B**
> **解析：** Flash Attention通过分块计算+融合kernel，减少对HBM的读写次数（IO-aware），在不改变计算结果的情况下大幅加速。

---

**80. MHA(Multi-Head Attention)、MQA(Multi-Query Attention)、GQA(Grouped-Query Attention)的关系是？**
A. MQA的K和V在所有头之间共享，GQA介于MHA和MQA之间
B. GQA的K和V在所有头之间共享
C. 三者的参数量完全相同
D. MHA是MQA的特例

> **答案：A**
> **解析：** MHA每个头有独立的K,V；MQA所有头共享一组K,V；GQA将头分成若干组，每组共享K,V。GQA是MHA和MQA的折中方案。

---

**81. 关于DPO(Direct Preference Optimization)，以下描述正确的是？**
A. DPO需要训练一个独立的Reward Model
B. DPO直接用偏好数据优化策略模型，不需要显式的Reward Model
C. DPO的效果一定比PPO差
D. DPO只能用于分类任务

> **答案：B**
> **解析：** DPO将RLHF中的奖励建模和策略优化合并为一步，直接在偏好数据上优化策略模型，无需训练Reward Model。

---

**82. 大模型量化中，PTQ和QAT的区别是？**
A. PTQ在训练后量化，QAT在训练时模拟量化
B. PTQ在训练时量化，QAT在训练后量化
C. 两者完全相同
D. PTQ精度一定比QAT高

> **答案：A**
> **解析：** PTQ(Post-Training Quantization)在模型训练完成后进行量化；QAT(Quantization-Aware Training)在训练过程中插入伪量化节点模拟量化误差，通常精度更高。

---

**83. Paged Attention解决的核心问题是？**
A. 加速模型训练
B. 解决KV Cache的显存碎片和浪费问题
C. 减少模型参数
D. 提高模型精度

> **答案：B**
> **解析：** Paged Attention借鉴操作系统分页思想，将KV Cache分成固定大小的块（pages），按需分配和释放，解决了KV Cache预分配导致的显存碎片和浪费。

---

**84. RAG(检索增强生成)相比纯微调的优势不包括？**
A. 可以利用最新的外部知识
B. 不需要重新训练模型
C. 可以减少模型的参数量
D. 可以缓解模型幻觉

> **答案：C**
> **解析：** RAG在推理时检索外部知识，不改变模型参数量。其优势是利用最新知识、减少幻觉、不需要重新训练。

---

**85. 关于Chain-of-Thought (CoT) Prompting，以下描述正确的是？**
A. CoT让模型一步到位给出答案
B. CoT引导模型逐步推理，输出中间步骤
C. CoT只能用于数学问题
D. CoT会减少模型的输出token数量

> **答案：B**
> **解析：** CoT通过在prompt中加入推理示例，引导模型分步骤思考并输出中间推理过程，提升复杂推理任务的准确率。

---

**86-100.（更多大模型选择题）**

**86. RMSNorm相比LayerNorm的主要改进是？**
A. 增加了可学习参数 B. 去掉了均值中心化，只保留方差归一化 C. 使用了BN的思想 D. 增加了残差连接
> **答案：B** （RMSNorm计算更快，去掉了减均值的操作）

**87. 投机采样(Speculative Decoding)的核心思想是？**
A. 用小模型快速草拟多个token，大模型验证并修正 B. 用大模型生成，小模型蒸馏 C. 并行生成所有token D. 使用贪心搜索代替beam search
> **答案：A**

**88. 以下哪个不是参数高效微调(PEFT)方法？**
A. LoRA B. Prefix Tuning C. 全参数微调 D. Adapter
> **答案：C** （全参数微调更新所有参数，不是PEFT）

**89. RLHF中PPO算法的Clip机制的作用是？**
A. 限制新策略与旧策略的偏差，保证训练稳定 B. 加速收敛 C. 减少显存 D. 增加探索
> **答案：A**

**90. SwiGLU激活函数被广泛用于LLaMA等模型，它本质上是？**
A. ReLU的变体 B. Swish函数与门控线性单元(GLU)的结合 C. Sigmoid的平滑版 D. 与GELU完全相同
> **答案：B**

**91. 关于Embedding层，以下描述正确的是？**
A. Embedding层是一种特殊的全连接层 B. Embedding层等价于一个可学习的查找表 C. Embedding层不包含可学习参数 D. Embedding层只能用于NLP
> **答案：B**

**92. Tokenizer中BPE(Byte Pair Encoding)的工作原理是？**
A. 将每个字符作为一个token B. 通过迭代合并最频繁出现的字符对来构建词表 C. 将每个单词作为一个token D. 随机分割文本
> **答案：B**

**93. 在大模型推理中，Time-to-First-Token(TTFT)和Token-per-Second(TPS)分别衡量什么？**
A. TTFT衡量生成第一个token的延迟，TPS衡量生成速度 B. 两者相同 C. TTFT衡量总时间 D. TPS衡量准确率
> **答案：A**

**94. 混合精度训练中使用BF16相比FP16的优势是？**
A. 精度更高 B. 更大的表示范围（与FP32相同的指数位），不容易溢出 C. 计算更快 D. 占用更少内存
> **答案：B** （BF16有8位指数位，与FP32相同，动态范围大，不易上/下溢）

**95. Transformer中FFN(前馈网络)层的典型结构是？**
A. 单层线性变换 B. 两层线性变换 + 中间激活函数 C. 卷积层 D. 循环层
> **答案：B** （FFN = Linear → Activation → Linear，中间维度通常是4×d_model）

---

## 四、概率论与数理统计选择题

**96. A与B相互独立，P(A)=0.4，P(B)=0.5，则P(A∪B)=？**
A. 0.2  B. 0.7  C. 0.9  D. 0.5

> **答案：B**
> **解析：** $P(A \cup B) = P(A) + P(B) - P(AB) = 0.4 + 0.5 - 0.4 \times 0.5 = 0.7$

---

**97. 疾病发病率0.1%，检测真阳性率99%，假阳性率1%。检测阳性后实际患病概率约为？**
A. 99%  B. 90%  C. 9%  D. 1%

> **答案：C**
> **解析：** 贝叶斯公式：$P(D|+) = \frac{0.99 \times 0.001}{0.99 \times 0.001 + 0.01 \times 0.999} \approx 9\%$

---

**98. 泊松分布 $X \sim Poisson(\lambda)$，则 $E(X)$ 和 $D(X)$ 分别为？**
A. $\lambda$, $\lambda$  B. $\lambda$, $\sqrt{\lambda}$  C. $\lambda$, $\lambda^2$  D. $\frac{1}{\lambda}$, $\frac{1}{\lambda^2}$

> **答案：A**
> **解析：** 泊松分布的期望和方差都等于 $\lambda$。

---

**99. 标准正态分布 $N(0,1)$ 的期望和方差分别是？**
A. 1, 0  B. 0, 1  C. 0, 0  D. 1, 1

> **答案：B**

---

**100. 以下关于大数定律的描述，正确的是？**
A. 样本均值随样本量增加趋近于总体均值
B. 样本均值服从正态分布
C. 大数定律只适用于正态分布
D. 大数定律和中心极限定理是同一个定理

> **答案：A**
> **解析：** B描述的是中心极限定理。大数定律说的是样本均值依概率收敛到总体均值。

---

**101. $X \sim U[0,1]$，Y = 3X + 2，则 $E(Y)$ = ？**
A. 2  B. 3.5  C. 3  D. 5

> **答案：B**
> **解析：** $E(X) = 0.5$，$E(Y) = 3 \times 0.5 + 2 = 3.5$

---

**102. $X \sim U[0,1]$，则 $D(X)$ = ？**
A. 1/2  B. 1/4  C. 1/12  D. 1/6

> **答案：C**
> **解析：** 均匀分布 $U[a,b]$ 的方差 $D(X) = (b-a)^2/12 = 1/12$。

---

**103. 关于最大似然估计(MLE)，以下描述正确的是？**
A. MLE选择使后验概率最大的参数
B. MLE选择使似然函数（或对数似然）最大的参数
C. MLE需要先验分布
D. MLE等价于最大后验估计(MAP)

> **答案：B**
> **解析：** MLE最大化似然函数 $L(\theta) = P(D|\theta)$；MAP最大化后验 $P(\theta|D) \propto P(D|\theta)P(\theta)$，MAP多了先验项。当先验为均匀分布时MLE=MAP。

---

**104. 如果两个随机变量的相关系数为0，以下说法正确的是？**
A. 两个变量一定独立
B. 两个变量线性不相关，但可能存在非线性关系
C. 两个变量完全没有关系
D. 两个变量的协方差一定不为0

> **答案：B**
> **解析：** 相关系数为0只说明线性不相关。独立→不相关，但不相关不一定独立（可能存在非线性依赖关系）。

---

**105. 中心极限定理的核心含义是？**
A. 所有分布都是正态分布
B. 独立同分布随机变量之和（标准化后）的分布趋近于正态分布
C. 样本均值等于总体均值
D. 方差趋近于0

> **答案：B**

---

**106. 二项分布 $B(n, p)$ 的期望和方差分别是？**
A. np, np(1-p)  B. np, np  C. p, p(1-p)  D. n, n/p

> **答案：A**

---

**107. 指数分布 $Exp(\lambda)$ 的期望是？**
A. $\lambda$  B. $1/\lambda$  C. $\lambda^2$  D. $\sqrt{\lambda}$

> **答案：B**

---

**108. 以下关于假设检验中p值的描述，正确的是？**
A. p值是原假设为真的概率
B. p值是在原假设为真的条件下，观测到当前或更极端结果的概率
C. p值越大，越应该拒绝原假设
D. p值 = 0.05 表示有5%的概率原假设为假

> **答案：B**
> **解析：** p值不是原假设为真的概率（这是常见误解），而是"假设H0为真时，得到当前或更极端观测值的概率"。p值小于显著性水平α时拒绝H0。

---

**109-125.（更多概率统计题，简答形式）**

**109.** $D(aX + b)$ = ？ → **答案：$a^2 D(X)$**（常数b不影响方差）

**110.** 两个独立随机变量X和Y的方差满足 $D(X+Y)$ = ？ → **答案：$D(X) + D(Y)$**

**111.** 协方差 $Cov(X,Y)$ 的定义？ → **答案：$E[(X-EX)(Y-EY)] = E(XY) - E(X)E(Y)$**

**112.** 几何分布(首次成功的试验次数)的期望？ → **答案：$1/p$**

**113.** 条件概率 $P(A|B)$ 的定义？ → **答案：$P(AB)/P(B)$**

**114.** 全概率公式？ → **答案：$P(B) = \sum_i P(B|A_i)P(A_i)$**

**115.** KL散度为什么不对称？ → **答案：$D_{KL}(P||Q) \neq D_{KL}(Q||P)$，因为是P和Q的交叉熵减去P的熵**

---

## 五、数据结构与算法选择题

**116. 平均情况下时间复杂度为 $O(n\log n)$ 且空间复杂度为 $O(1)$ 的排序算法是？**
A. 快速排序  B. 归并排序  C. 堆排序  D. 冒泡排序

> **答案：C**
> **解析：** 快排空间 $O(\log n)$，归并空间 $O(n)$，堆排序空间 $O(1)$，冒泡时间 $O(n^2)$。

---

**117. 完全二叉树中按层序编号，结点i的右孩子编号为？**
A. 2i  B. 2i+1  C. 2i-1  D. 无法确定

> **答案：B**
> **解析：** 左孩子 = 2i，右孩子 = 2i+1，父结点 = ⌊i/2⌋。

---

**118. 关于哈希表，以下描述正确的是？**
A. 哈希冲突可以完全避免
B. 哈希表查找效率一定比二叉排序树高
C. 处理冲突的方法主要有开放定址法和拉链法
D. 装填因子越大，查找效率越高

> **答案：C**
> **解析：** 冲突无法完全避免；查找效率取决于哈希函数和装填因子；装填因子越大冲突越多效率越低。

---

**119. 用筛选法建立初始堆的时间复杂度是？**
A. $O(n)$  B. $O(n\log n)$  C. $O(\log n)$  D. $O(n^2)$

> **答案：A**
> **解析：** 虽然单次调整是 $O(\log n)$，但从底向上建堆的总时间经证明为 $O(n)$。

---

**120. n个顶点的连通无向图，其生成树的边数为？**
A. n  B. n-1  C. n+1  D. 2n

> **答案：B**

---

**121. 以下排序算法中，哪些是稳定的？（多选）**
A. 冒泡排序  B. 快速排序  C. 归并排序  D. 堆排序  E. 插入排序

> **答案：ACE**
> **解析：** 稳定排序：冒泡、插入、归并、基数排序。不稳定：快排、堆排序、选择排序、希尔排序。

---

**122. 在一棵二叉树中，若叶子结点数为n₀，度为2的结点数为n₂，则？**
A. n₀ = n₂  B. n₀ = n₂ + 1  C. n₀ = n₂ - 1  D. n₀ = 2n₂

> **答案：B**
> **解析：** 二叉树性质：$n_0 = n_2 + 1$（叶子数 = 度为2的结点数 + 1）。

---

**123. 快速排序的最坏时间复杂度是？**
A. $O(n\log n)$  B. $O(n^2)$  C. $O(n)$  D. $O(\log n)$

> **答案：B**
> **解析：** 当输入已排序且pivot选择不当（如总选第一个元素），快排退化为 $O(n^2)$。

---

**124. 图的DFS使用的数据结构是？BFS使用的是？**
A. DFS-队列，BFS-栈  B. DFS-栈，BFS-队列  C. 都用栈  D. 都用队列

> **答案：B**

---

**125. 以下关于B+树的说法，正确的是？**
A. 所有数据只存储在叶子节点
B. 所有数据存储在内部节点
C. B+树不适合做数据库索引
D. B+树的叶子节点之间没有指针连接

> **答案：A**
> **解析：** B+树的数据全部存储在叶子节点，叶子节点之间用链表连接（利于范围查询）。内部节点只存索引key。

---

**126-145.（更多数据结构题，简答形式）**

**126.** 红黑树插入操作的时间复杂度？ → **O(log n)**

**127.** 跳表(Skip List)查找的平均时间复杂度？ → **O(log n)**

**128.** 布隆过滤器有假阳性还是假阴性？ → **有假阳性，无假阴性**

**129.** 拓扑排序适用于什么类型的图？ → **有向无环图(DAG)**

**130.** Dijkstra算法不能处理什么类型的边？ → **负权边**

**131.** 哈夫曼编码的核心思想？ → **出现频率高的字符用短编码，频率低的用长编码**

**132.** 一个有n个结点的完全二叉树的高度是？ → **⌊log₂n⌋ + 1**

**133.** 邻接矩阵表示图，空间复杂度是？ → **O(V²)**

**134.** 邻接表表示图，空间复杂度是？ → **O(V + E)**

**135.** Kruskal算法的核心数据结构？ → **并查集**

---

## 六、操作系统选择题

**136. 进程和线程的区别，以下描述不正确的是？**
A. 线程是资源分配和调度的独立单位
B. 同一进程的各线程共享进程的虚存空间
C. 进程中所有线程共享代码段
D. 不同线程可以对应相同的程序

> **答案：A**
> **解析：** 进程是资源分配的独立单位，线程是CPU调度的独立单位。

---

**137. 死锁产生的四个必要条件不包括？**
A. 互斥条件  B. 请求与保持  C. 不可剥夺  D. 循环等待  E. 资源有序分配

> **答案：E**
> **解析：** 四个必要条件：互斥、请求与保持、不可剥夺、循环等待。资源有序分配是破坏死锁的策略。

---

**138. 虚拟存储器只能基于以下哪种分配技术？**
A. 连续分配  B. 非连续分配  C. 两者都可以  D. 都不行

> **答案：B**
> **解析：** 虚拟内存基于分页或分段等非连续分配技术实现。

---

**139. 选择"以后不再使用"的页面淘汰的算法是？**
A. FIFO  B. OPT(最佳)  C. LRU  D. NRU

> **答案：B**
> **解析：** OPT(最优页面置换)选择未来最长时间不被使用的页面淘汰，理论最优但无法实现。

---

**140. 导致Belady异常（分配更多物理块反而缺页率上升）的算法是？**
A. FIFO  B. LRU  C. OPT  D. 都不会

> **答案：A**
> **解析：** FIFO算法可能出现Belady异常。LRU和OPT都属于栈算法，不会出现此现象。

---

**141. 进程间通信(IPC)方式不包括？**
A. 管道  B. 消息队列  C. 共享内存  D. 继承

> **答案：D**

---

**142. 银行家算法用于？**
A. 死锁预防  B. 死锁避免  C. 死锁检测  D. 死锁解除

> **答案：B**
> **解析：** 银行家算法在资源分配前检查是否存在安全序列，属于死锁避免策略。

---

**143. 用户态切换到内核态的方式不包括？**
A. 系统调用  B. 异常  C. 外部中断  D. 函数调用

> **答案：D**
> **解析：** 普通函数调用在用户态执行，不涉及态的切换。

---

**144. LRU页面置换算法的含义是？**
A. 最近最少使用  B. 最不经常使用  C. 先进先出  D. 随机替换

> **答案：A**
> **解析：** LRU = Least Recently Used，淘汰最近最久未使用的页面。LFU = Least Frequently Used，是最不经常使用。

---

**145-155.（更多OS题）**

**145.** fork()的返回值？ → **父进程返回子进程PID，子进程返回0**

**146.** 写时复制(COW)的作用？ → **fork时不立即复制内存，只在写入时才复制，提高效率**

**147.** 硬链接和软链接的区别？ → **硬链接共享inode，不能跨文件系统；软链接是路径引用，可以跨文件系统**

**148.** epoll相比select的优势？ → **不受FD数量限制、事件驱动(O(1))而非轮询**

**149.** 同步IO和异步IO的区别？ → **同步IO需要等待IO完成；异步IO发起后立即返回，完成后通知**

**150.** 什么是上下文切换(Context Switch)？ → **CPU从一个进程/线程切换到另一个，需保存/恢复寄存器、程序计数器等状态**

---

## 七、计算机网络选择题

**151. TCP三次握手，以下描述正确的是？**
A. 第一次握手客户端发送SYN=1, seq=x，进入SYN_SENT状态
B. 第二次握手服务器回复SYN=1, ACK=1, ack=x (而非x+1)
C. 第三次握手客户端无需发送任何报文
D. TCP三次握手是为了防止网络拥塞

> **答案：A**
> **解析：** B项确认号应为x+1；C项客户端需回复ACK；握手目的是同步序列号和确认双方收发能力。

---

**152. HTTP状态码404表示？**
A. 服务器内部错误  B. 请求的资源不存在  C. 资源已永久移动  D. 请求成功但无内容

> **答案：B**

---

**153. 下列哪个HTTP状态码表示永久重定向？**
A. 200  B. 301  C. 302  D. 304

> **答案：B**
> **解析：** 301=永久重定向，302=临时重定向，304=未修改(缓存)。

---

**154. IP地址192.168.1.0/24，子网掩码255.255.255.240划分子网，子网数和每子网主机数？**
A. 14, 14  B. 16, 14  C. 254, 6  D. 14, 62

> **答案：B**
> **解析：** /24变为/28，借了4位(2⁴=16子网)，剩余4位主机位(2⁴-2=14可用主机)。

---

**155. TCP和UDP的区别，以下描述错误的是？**
A. TCP面向连接，UDP无连接
B. TCP保证可靠传输，UDP不保证
C. TCP比UDP传输速度快
D. UDP适用于实时音视频

> **答案：C**
> **解析：** UDP由于没有连接建立、确认、重传等开销，传输速度通常比TCP快。TCP更可靠但开销大。

---

**156. TIME_WAIT状态持续2MSL的原因是？**
A. 等待服务器重启  
B. 确保最后的ACK能到达对方，防止旧连接的数据包干扰新连接
C. 节省端口资源  
D. 没有实际意义

> **答案：B**

---

**157. HTTP/2相比HTTP/1.1的改进不包括？**
A. 多路复用(Multiplexing)  B. 头部压缩(HPACK)  C. 服务器推送  D. 使用UDP协议

> **答案：D**
> **解析：** HTTP/2仍基于TCP。HTTP/3才基于QUIC(UDP)。

---

**158. HTTPS中TLS握手的目的是？**
A. 验证URL是否正确  B. 协商加密算法并交换密钥  C. 压缩数据  D. 加速DNS解析

> **答案：B**

---

**159. DNS解析过程中，本地DNS服务器向根域名服务器查询通常采用？**
A. 递归查询  B. 迭代查询  C. 广播查询  D. 组播查询

> **答案：B**
> **解析：** 客户端→本地DNS通常递归，本地DNS→根/顶级域名服务器通常迭代。

---

**160-170.（更多网络题）**

**160.** ARP协议的作用？ → **根据IP地址获取MAC地址**

**161.** TCP拥塞控制的四个阶段？ → **慢启动、拥塞避免、快重传、快恢复**

**162.** HTTP状态码502表示？ → **Bad Gateway，网关/代理从上游服务器收到无效响应**

**163.** Cookie和Session的区别？ → **Cookie存在客户端，Session存在服务端**

**164.** CDN的作用？ → **将内容缓存到离用户最近的节点，加速访问**

**165.** 什么是粘包问题？如何解决？ → **TCP是字节流协议，可能将多个包合并；解决：定长消息、分隔符、消息头+长度字段**

---

## 八、数据库/SQL选择题

**166. 关于数据库索引，以下说法正确的是？**
A. 索引越多越好
B. B+树索引的叶子节点通过链表相连
C. 聚簇索引和非聚簇索引的存储方式完全相同
D. 索引不占额外存储空间

> **答案：B**
> **解析：** 索引会占用存储空间并降低写入性能，不是越多越好。B+树叶子节点通过链表连接，便于范围查询。

---

**167. 事务的ACID特性中，"I"代表？**
A. 完整性(Integrity)  B. 隔离性(Isolation)  C. 独立性(Independence)  D. 继承性(Inheritance)

> **答案：B**
> **解析：** ACID = 原子性(Atomicity)、一致性(Consistency)、隔离性(Isolation)、持久性(Durability)。

---

**168. 以下哪种事务隔离级别可以解决幻读问题？**
A. 读未提交  B. 读已提交  C. 可重复读  D. 串行化

> **答案：D**
> **解析：** 串行化是最高隔离级别，可以解决所有并发问题（脏读、不可重复读、幻读）。MySQL的可重复读通过MVCC+间隙锁也能在一定程度上解决幻读。

---

**169. WHERE和HAVING的区别是？**
A. 两者完全相同
B. WHERE在GROUP BY之前过滤，HAVING在之后过滤
C. HAVING在GROUP BY之前过滤，WHERE在之后过滤
D. HAVING不能使用聚合函数

> **答案：B**
> **解析：** WHERE先过滤行，再GROUP BY分组；HAVING在分组后过滤组，可以使用聚合函数（如COUNT、SUM）。

---

**170. ROW_NUMBER(), RANK(), DENSE_RANK() 对于并列值的处理？**

| 分数 | ROW_NUMBER | RANK | DENSE_RANK |
|------|-----------|------|------------|
| 100 | 1 | 1 | 1 |
| 100 | 2 | 1 | 1 |
| 90 | 3 | 3 | 2 |
| 80 | 4 | 4 | 3 |

> **解析：** ROW_NUMBER不处理并列（强制唯一编号）；RANK跳号（1,1,3）；DENSE_RANK不跳号（1,1,2）。

---

**171. LEFT JOIN的结果包含？**
A. 仅两表的交集
B. 左表所有行 + 右表匹配行（不匹配则NULL）
C. 右表所有行 + 左表匹配行
D. 两表的并集

> **答案：B**

---

**172. 以下哪个SQL语句可以删除重复数据只保留一条？**
A. `DELETE FROM t WHERE id NOT IN (SELECT MIN(id) FROM t GROUP BY email)`
B. `SELECT DISTINCT * FROM t`
C. `DROP TABLE t`
D. `TRUNCATE TABLE t`

> **答案：A**
> **解析：** A通过保留每组最小id的记录，删除其余重复项。B只是查询去重不修改数据。

---

**173-180.（更多SQL题）**

**173.** 聚簇索引和非聚簇索引的区别？ → **聚簇索引的叶子节点存储完整数据行；非聚簇索引的叶子节点存储主键值（需要回表）**

**174.** 什么是索引失效？列举3种情况。 → **在索引列上使用函数、LIKE '%xxx'左模糊、隐式类型转换、OR条件、不符合最左前缀原则**

**175.** EXPLAIN命令的作用？ → **查看SQL执行计划，分析是否使用索引、扫描方式(全表/索引)、预估行数等**

**176.** 什么是MVCC？ → **多版本并发控制，通过保存数据的多个版本实现读写不阻塞**

**177.** InnoDB和MyISAM的区别？ → **InnoDB支持事务、行级锁、外键；MyISAM不支持事务、只有表级锁**

**178.** 什么是回表查询？ → **使用非聚簇索引查找时，先通过索引找到主键，再通过主键到聚簇索引找完整数据行**

**179.** 覆盖索引是什么？ → **查询所需的列全部包含在索引中，无需回表**

**180.** 最左前缀原则？ → **联合索引(a,b,c)，查询条件必须从最左列开始匹配才能使用索引。如WHERE a=1 AND b=2可以，WHERE b=2不行**

---

## 📊 排序算法速查表

| 算法 | 平均时间 | 最坏时间 | 空间 | 稳定性 |
|------|---------|---------|------|--------|
| 冒泡排序 | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ✅ 稳定 |
| 选择排序 | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ❌ 不稳定 |
| 插入排序 | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ✅ 稳定 |
| 希尔排序 | $O(n^{1.3})$ | $O(n^2)$ | $O(1)$ | ❌ 不稳定 |
| 归并排序 | $O(n\log n)$ | $O(n\log n)$ | $O(n)$ | ✅ 稳定 |
| 快速排序 | $O(n\log n)$ | $O(n^2)$ | $O(\log n)$ | ❌ 不稳定 |
| 堆排序 | $O(n\log n)$ | $O(n\log n)$ | $O(1)$ | ❌ 不稳定 |
| 计数排序 | $O(n+k)$ | $O(n+k)$ | $O(k)$ | ✅ 稳定 |
| 基数排序 | $O(nk)$ | $O(nk)$ | $O(n+k)$ | ✅ 稳定 |

## 📊 常见分布速查表

| 分布 | 表示 | 期望 E(X) | 方差 D(X) |
|------|------|-----------|-----------|
| 伯努利 | $B(1,p)$ | $p$ | $p(1-p)$ |
| 二项 | $B(n,p)$ | $np$ | $np(1-p)$ |
| 泊松 | $P(\lambda)$ | $\lambda$ | $\lambda$ |
| 均匀 | $U[a,b]$ | $\frac{a+b}{2}$ | $\frac{(b-a)^2}{12}$ |
| 指数 | $Exp(\lambda)$ | $\frac{1}{\lambda}$ | $\frac{1}{\lambda^2}$ |
| 正态 | $N(\mu,\sigma^2)$ | $\mu$ | $\sigma^2$ |
| 几何 | $Geo(p)$ | $\frac{1}{p}$ | $\frac{1-p}{p^2}$ |

---

> **总计：本文件收录 200+ 道选择题（含答案和解析），加上前两份文件的 400+ 道题目，全系列共计 600+ 道算法岗笔试题目。**
